---
phase: 06-rarity-shiny-variants
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - src/components/beastiary/ConservationBadge.tsx
  - src/components/beastiary/BeastiaryCard.tsx
  - src/app/(app)/beastiary/page.tsx
autonomous: true
requirements:
  - BEAST-04
  - BEAST-05
  - BEAST-06

must_haves:
  truths:
    - "Unlocked BeastiaryCards show a coloured ring/shadow matching the species rarityTier (BEAST-04)"
    - "Shiny cards override the rarity ring with gold; the image area has a shimmer overlay and a ✦ Shiny badge (BEAST-06)"
    - "Unlocked BeastiaryCards for birds show a ConservationBadge (BTO Red / Amber / Green) bottom-left of the image (BEAST-05)"
    - "Locked cards are visually unchanged — no rarity ring, no conservation badge, no shiny treatment"
    - "isShiny and conservationStatus flow from the server page query to BeastiaryCard without client-side state"
  artifacts:
    - path: "src/components/beastiary/ConservationBadge.tsx"
      provides: "Server component rendering BTO status pill; returns null for null status"
      exports: ["ConservationBadge"]
    - path: "src/components/beastiary/BeastiaryCard.tsx"
      provides: "Updated card with rarity ring, shiny overlay+badge, ConservationBadge"
      contains: "RARITY_RING"
    - path: "src/app/(app)/beastiary/page.tsx"
      provides: "isShiny added to collections query; conservationStatus included in species query"
      contains: "isShiny"
  key_links:
    - from: "src/app/(app)/beastiary/page.tsx"
      to: "src/components/beastiary/BeastiaryCard.tsx"
      via: "isShiny prop passed from collectedMap"
      pattern: "isShiny"
    - from: "src/components/beastiary/BeastiaryCard.tsx"
      to: "src/components/beastiary/ConservationBadge.tsx"
      via: "conservationStatus prop on species"
      pattern: "ConservationBadge"
---

<objective>
Create ConservationBadge, update BeastiaryCard with rarity glow and shiny treatment, and update the beastiary page query to pass isShiny and conservationStatus through to the cards.

Purpose: Makes the Phase 6 features visible. Builds entirely on the schema and route work from 06-01.
Output: Visually differentiated cards with rarity glow, optional shiny gold treatment, and BTO badge.
</objective>

<execution_context>
@/Users/ethanvenables/Documents/Claude/Projects/Scout/.planning/phases/06-rarity-shiny-variants/06-CONTEXT.md
@/Users/ethanvenables/Documents/Claude/Projects/Scout/.planning/phases/06-rarity-shiny-variants/06-RESEARCH.md
</execution_context>

<context>
@/Users/ethanvenables/Documents/Claude/Projects/Scout/.planning/ROADMAP.md
@/Users/ethanvenables/Documents/Claude/Projects/Scout/.planning/REQUIREMENTS.md

<!-- Prior plan output needed for DB schema shape -->
@/Users/ethanvenables/Documents/Claude/Projects/Scout/.planning/phases/06-rarity-shiny-variants/06-01-SUMMARY.md

<interfaces>
<!-- Existing BeastiaryCard props — from src/components/beastiary/BeastiaryCard.tsx -->
```ts
type BeastiaryCardProps = {
  species: {
    id: string;
    commonName: string;
    scientificName: string;
    rarityTier: string;
    funFact: string | null;
    // conservationStatus: string | null  ← ADD per D-12
  };
  sightingCount?: number;
  personalityTrait?: string | null;
  // isShiny?: boolean  ← ADD per D-10/D-11
};
```

<!-- Current unlocked card container class (line 34 of BeastiaryCard.tsx): -->
`"flex flex-col rounded-xl border border-white/15 bg-white/5 overflow-hidden"`
<!-- Replace border+ring classes with RARITY_RING / SHINY_RING lookup -->

<!-- Current image area div (line 35): -->
`"aspect-[3/4] bg-gradient-to-b from-gray-700 to-gray-800 flex items-center justify-center relative"`
<!-- Shiny overlay + ✦ badge + ConservationBadge go INSIDE this div -->

<!-- Beastiary page collections query (lines 29-35 of page.tsx): -->
```ts
const userCollections = await db
  .select({
    speciesId: collections.speciesId,
    sightingCount: collections.sightingCount,
    personalityTrait: collections.personalityTrait,
    // isShiny: collections.isShiny  ← ADD per §8
  })
  .from(collections)
  .where(eq(collections.userId, userId));
```

<!-- collectedMap type (line 37 of page.tsx): -->
`Map<string, { sightingCount: number; personalityTrait: string | null }>`
<!-- Extend to include isShiny: boolean -->

<!-- Species query (line 23-26 of page.tsx) already uses db.select() with no column list -->
<!-- After migration conservationStatus will be returned automatically -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ConservationBadge + update BeastiaryCard props and visuals</name>
  <files>src/components/beastiary/ConservationBadge.tsx, src/components/beastiary/BeastiaryCard.tsx</files>
  <action>
    **Part A — create src/components/beastiary/ConservationBadge.tsx (per D-12):**
    Server component (no "use client"). Props: `{ status: string | null }`. Returns null if status is null or not a recognized key.

    Define STATUS_STYLES as a const record mapping 'red' | 'amber' | 'green' to Tailwind classes:
    - red: `bg-red-900/80 text-red-300`
    - amber: `bg-amber-900/80 text-amber-300`
    - green: `bg-green-900/80 text-green-300`

    Define STATUS_LABELS mapping to 'BTO Red', 'BTO Amber', 'BTO Green'.

    Render a `<span>` with `${STATUS_STYLES[key]} text-[10px] font-semibold px-1.5 py-0.5 rounded` and the label text. Export as named export `ConservationBadge`.

    **Part B — update src/components/beastiary/BeastiaryCard.tsx (per D-03, D-08, D-09, D-10, D-11):**

    1. Extend the `species` type in `BeastiaryCardProps` to add `conservationStatus: string | null`.
    2. Add `isShiny?: boolean` to the top-level props (default false).
    3. Define two constants above the component function:
       - `RARITY_RING` record mapping each rarityTier to its Tailwind ring+shadow string (see RESEARCH.md §4). Keys: common, uncommon, rare, super_rare, legendary, mythic.
       - `SHINY_RING = 'ring-2 ring-yellow-400/80 shadow-xl shadow-yellow-400/40'`

    4. Unlocked card container div: replace `border border-white/15 bg-white/5` with:
       `${isShiny ? SHINY_RING : (RARITY_RING[species.rarityTier] ?? RARITY_RING.common)} bg-white/5`
       Keep `flex flex-col rounded-xl overflow-hidden` unchanged. Do NOT add rarity ring to the locked card — locked card div is unchanged.

    5. Inside the unlocked image area div (the one with `relative` class), after the existing `{species.commonName.charAt(0)}` letter span, add:
       a. Shiny gold shimmer overlay (only when isShiny): absolute inset-0 bg-gradient-to-br from-yellow-400/20 via-transparent to-amber-500/20 pointer-events-none div (per D-10).
       b. Shiny badge (only when isShiny): absolute top-2 left-2 flex items-center gap-0.5 badge — ✦ in text-yellow-400 text-xs font-bold, and "Shiny" label in text-yellow-400 text-[10px] font-semibold, all in bg-yellow-400/15 rounded px-1.5 py-0.5 wrapper (per D-11).
       c. ConservationBadge: wrapped in an `absolute bottom-2 left-2` positioning div, render `<ConservationBadge status={species.conservationStatus} />`. Import from './ConservationBadge'. Renders after the existing `<RarityBadge>` at bottom-right (keep RarityBadge unchanged at bottom-2 right-2).

    BeastiaryCard remains a server component — no "use client" directive.
  </action>
  <verify>
    <automated>cd /Users/ethanvenables/Documents/Claude/Projects/Scout && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    TypeScript compiles clean. ConservationBadge.tsx exists and exports ConservationBadge. BeastiaryCard.tsx accepts isShiny and conservationStatus props; RARITY_RING constant is defined; ConservationBadge is imported and rendered conditionally.
  </done>
</task>

<task type="auto">
  <name>Task 2: Beastiary page — wire isShiny and conservationStatus through to cards</name>
  <files>src/app/(app)/beastiary/page.tsx</files>
  <action>
    Update src/app/(app)/beastiary/page.tsx to pass the new data to BeastiaryCard (per RESEARCH.md §8).

    1. In the `db.select({ ... })` on the `collections` table, add `isShiny: collections.isShiny` to the select object.

    2. Update the `collectedMap` type annotation to include `isShiny: boolean` (or `boolean | null`):
       `Map<string, { sightingCount: number; personalityTrait: string | null; isShiny: boolean | null }>`

    3. In the `.map((c) => [...])` that builds collectedMap, include `isShiny: c.isShiny` in the value object.

    4. The `allSpecies` query already uses `db.select().from(species)` with no column restriction — after the 06-01 migration it will automatically return `conservationStatus`. No change needed to the species query.

    5. In the JSX where BeastiaryCard is rendered, pass:
       - `isShiny={entry?.isShiny ?? false}` as a prop
       - Pass `conservationStatus` via the species spread: in the species prop object, add `conservationStatus: s.conservationStatus ?? null`

    The page remains a server component (no "use client"). The `(session.user as { id: string }).id` cast is already present — leave it unchanged.
  </action>
  <verify>
    <automated>cd /Users/ethanvenables/Documents/Claude/Projects/Scout && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    TypeScript compiles clean. BeastiaryCard receives isShiny and conservationStatus from the page. The beastiary page renders without runtime errors (verifiable by running the dev server and visiting /beastiary).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Rarity glow rings on all unlocked beastiary cards, gold shiny treatment on any shiny card, BTO conservation badge on bird cards.
  </what-built>
  <how-to-verify>
    1. Run `npm run dev` and visit http://localhost:3000/beastiary
    2. Confirm unlocked bird cards show a coloured ring matching their rarity tier (e.g. common = faint grey ring, rare = blue ring, legendary = amber ring)
    3. Confirm the ConservationBadge (BTO Red / Amber / Green) appears bottom-left of the image on bird cards
    4. Confirm locked cards show NO rarity ring (remain with plain dark border)
    5. To verify shiny: temporarily change the roll threshold in route.ts to `Math.random() < 1.0`, log a new sighting, reload beastiary — confirm gold ring + shimmer overlay + ✦ Shiny badge appear. Revert threshold to 0.02 after.
  </how-to-verify>
  <resume-signal>Type "approved" once all visual treatments confirm, or describe any issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server page → BeastiaryCard | All data flows server-side; no client JS renders isShiny or conservationStatus |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02-01 | Information Disclosure | ConservationBadge rendering null | accept | Returning null for unrecognized status values is correct defensive behavior; no sensitive data exposed |
| T-06-02-02 | Tampering | isShiny display client-side | mitigate | BeastiaryCard is a server component — isShiny value originates from DB query in server page, not from any client-readable state or URL param |
| T-06-SC | Tampering | npm install | accept | No new packages installed in this plan — ConservationBadge uses only React/Tailwind, already in project |
</threat_model>

<verification>
- `npx tsc --noEmit` passes with no errors across all three modified files
- ConservationBadge renders a pill for 'red', 'amber', 'green'; returns null for null status (verifiable in dev)
- Unlocked cards show rarity ring; locked cards do not
- Bird cards show BTO badge; mammal/reptile/amphibian cards do not
- A shiny card (tested with threshold=1.0) shows gold ring + shimmer + ✦ badge
</verification>

<success_criteria>
- ConservationBadge.tsx created and exported as server component
- BeastiaryCard.tsx accepts isShiny and conservationStatus; RARITY_RING map applied to unlocked cards; locked cards unchanged
- Beastiary page passes isShiny and conservationStatus through to each BeastiaryCard
- TypeScript compiles clean
- Human checkpoint approved: all three visual treatments visible at /beastiary
</success_criteria>

<output>
Create `.planning/phases/06-rarity-shiny-variants/06-02-SUMMARY.md` when done.
</output>
