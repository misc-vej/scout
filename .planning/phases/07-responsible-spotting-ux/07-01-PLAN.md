---
phase: 07-responsible-spotting-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/db/schema.ts
  - src/app/api/pledge/accept/route.ts
  - src/components/auth/PledgeModal.tsx
  - src/app/(app)/layout.tsx
autonomous: true
requirements:
  - RESP-01

must_haves:
  truths:
    - "A user without a pledge sees a full-screen, non-dismissible modal on every app page"
    - "Clicking 'I Accept the Pledge' POSTs to /api/pledge/accept and removes the modal without a full page reload"
    - "After acceptance, pledgeAcceptedAt is a non-null timestamp on the user's profile row"
    - "A user who has already accepted sees no modal — app content renders normally"
  artifacts:
    - path: "src/lib/db/schema.ts"
      provides: "pledgeAcceptedAt column on profiles, spottingTips column on species"
      contains: "pledge_accepted_at"
    - path: "src/app/api/pledge/accept/route.ts"
      provides: "POST endpoint that stamps pledgeAcceptedAt"
      exports: ["POST"]
    - path: "src/components/auth/PledgeModal.tsx"
      provides: "Full-screen client component with pledge text and accept button"
    - path: "src/app/(app)/layout.tsx"
      provides: "Server-side pledge gate that renders PledgeModal if needed"
  key_links:
    - from: "src/app/(app)/layout.tsx"
      to: "src/components/auth/PledgeModal.tsx"
      via: "conditional JSX render when pledgeAcceptedAt is null"
    - from: "src/components/auth/PledgeModal.tsx"
      to: "/api/pledge/accept"
      via: "useMutation POST fetch on button click"
    - from: "src/app/api/pledge/accept/route.ts"
      to: "profiles.pledgeAcceptedAt"
      via: "db.update(profiles).set({ pledgeAcceptedAt: new Date() })"
---

<objective>
Add the Spotter's Pledge gate: two new schema columns (pledgeAcceptedAt on profiles, spottingTips on species), a pledge-accept API route, a full-screen PledgeModal client component, and a server-side layout gate. Users must accept the pledge before any app content is accessible.

Purpose: Fulfils RESP-01. Ensures every Scout user has committed to responsible wildlife observation before using collection features.
Output: Migrated DB with two new columns, /api/pledge/accept endpoint, PledgeModal component, updated layout.tsx.
</objective>

<execution_context>
@/Users/ethanvenables/Documents/Claude/Projects/Scout/.planning/phases/07-responsible-spotting-ux/07-RESEARCH.md
</execution_context>

<context>
@/Users/ethanvenables/Documents/Claude/Projects/Scout/.planning/PROJECT.md
@/Users/ethanvenables/Documents/Claude/Projects/Scout/.planning/ROADMAP.md

@/Users/ethanvenables/Documents/Claude/Projects/Scout/src/lib/db/schema.ts
@/Users/ethanvenables/Documents/Claude/Projects/Scout/src/app/(app)/layout.tsx
</context>

<interfaces>
<!-- Key contracts the executor needs. -->

From src/lib/db/schema.ts (current profiles table):
```typescript
export const profiles = pgTable("profiles", {
  id: uuid("id").primaryKey().defaultRandom(),
  userId: uuid("user_id").notNull().unique().references(() => users.id, { onDelete: "cascade" }),
  displayName: text("display_name"),
  passkeyPromptedAt: timestamp("passkey_prompted_at", { mode: "date" }),
  createdAt: timestamp("created_at").defaultNow().notNull(),
  updatedAt: timestamp("updated_at").defaultNow().notNull(),
});
```

From src/app/(app)/layout.tsx (current structure):
```typescript
// Imports: auth, redirect, db, users (schema), eq (drizzle-orm)
// Also imports: NavShell, EmailVerificationBanner, QueryProvider
// Pattern: await auth() → redirect if no session → db.select from users → return JSX wrapping <QueryProvider>/<EmailVerificationBanner>/<NavShell>
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Schema columns + migration</name>
  <files>src/lib/db/schema.ts</files>
  <action>
    Edit src/lib/db/schema.ts. Make two additions in a single edit:

    1. Add `pledgeAcceptedAt` to the `profiles` table (after passkeyPromptedAt):
       `pledgeAcceptedAt: timestamp("pledge_accepted_at", { mode: "date" }),`
       The column is nullable — no `.notNull()`. (per D-01)

    2. Add `spottingTips` to the `species` table (after funFact):
       `spottingTips: text("spotting_tips"),`
       The column is nullable — no `.notNull()`. (per D-07)

    Then run the migration from the project root:
    ```
    export $(grep -v '^#' .env.local | xargs)
    npx drizzle-kit generate
    DATABASE_URL=$DATABASE_URL_UNPOOLED npx drizzle-kit migrate
    ```

    Both columns must land in a single migration run. Do not run two separate migrations.
  </action>
  <verify>
    <automated>grep -n "pledge_accepted_at\|spotting_tips" src/lib/db/schema.ts | wc -l | xargs -I{} test {} -ge 2 && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>schema.ts contains both pledge_accepted_at (on profiles) and spotting_tips (on species); migration ran without errors; npx drizzle-kit generate produced a migration file.</done>
</task>

<task type="auto">
  <name>Task 2: POST /api/pledge/accept</name>
  <files>src/app/api/pledge/accept/route.ts</files>
  <action>
    Create the directory src/app/api/pledge/accept/ and the file route.ts.

    Implementation (per D-03 and RESEARCH.md §2):
    - Import NextResponse from next/server, auth from @/auth, db from @/lib/db, profiles from @/lib/db/schema, eq from drizzle-orm.
    - Export async function POST() (no request parameter needed).
    - Call `const session = await auth()`. If `!session?.user?.id`, return NextResponse.json({ error: 'Unauthorized' }, { status: 401 }).
    - Cast to get userId: `const userId = (session.user as { id: string }).id;`
    - Run: `await db.update(profiles).set({ pledgeAcceptedAt: new Date() }).where(eq(profiles.userId, userId));`
    - Return NextResponse.json({ accepted: true }).

    No request body parsing required — this endpoint has no inputs beyond the authenticated user identity.
  </action>
  <verify>
    <automated>test -f src/app/api/pledge/accept/route.ts && grep -q "pledgeAcceptedAt" src/app/api/pledge/accept/route.ts && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>src/app/api/pledge/accept/route.ts exists; exports POST; updates profiles.pledgeAcceptedAt with a new Date(); returns { accepted: true }; returns 401 for unauthenticated requests.</done>
</task>

<task type="auto">
  <name>Task 3: PledgeModal client component + layout gate</name>
  <files>src/components/auth/PledgeModal.tsx, src/app/(app)/layout.tsx</files>
  <action>
    Step A — Create src/components/auth/PledgeModal.tsx (per D-04, D-05, D-06 and RESEARCH.md §3):

    - File must start with `'use client';`
    - Import useState from react and useMutation from @tanstack/react-query.
    - Export named function `PledgeModal` (no props).
    - State: `const [accepted, setAccepted] = useState(false);`
    - Mutation: POST to '/api/pledge/accept' with no body. On success, call `setAccepted(true)`.
    - If `accepted` is true, return null — removes modal from DOM without reload. (per D-05)
    - Render a fixed full-screen overlay: `className="fixed inset-0 z-50 flex items-center justify-center bg-black/80 backdrop-blur-sm p-4"`. No close/dismiss button anywhere. (per D-04)
    - Inside: white/dark modal card, h2 "The Scout's Pledge", pledge text paragraph from D-06 verbatim:
      "I promise to observe wildlife without disturbing it. I will keep a safe distance, never touch nests or young, stay on paths, and follow the Wildlife & Countryside Act 1981. If I see something rare, I'll keep the location to myself."
    - Single button: "I Accept the Pledge". Disabled when mutation.isPending. Shows "Saving…" while pending.
    - Use RESEARCH.md §3 for exact class names and layout — follow it faithfully.

    Step B — Update src/app/(app)/layout.tsx (per D-02 and RESEARCH.md §4):

    Read the existing layout first. It currently:
    - Imports auth, redirect, db, users (from schema), eq (drizzle-orm), NavShell, EmailVerificationBanner, QueryProvider.
    - Selects emailVerified from users. Wraps content in QueryProvider > EmailVerificationBanner + NavShell.

    Add to the existing layout WITHOUT removing or reordering any existing logic:
    1. Add new imports at the top: `import { profiles } from '@/lib/db/schema';` and `import { PledgeModal } from '@/components/auth/PledgeModal';`
    2. After the existing `const [user] = await db.select(...)` block, add a second query:
       ```
       const [profile] = await db
         .select({ pledgeAcceptedAt: profiles.pledgeAcceptedAt })
         .from(profiles)
         .where(eq(profiles.userId, session.user.id))
         .limit(1);
       const pledgeAccepted = !!(profile?.pledgeAcceptedAt);
       ```
       Note: session.user.id is already available from the existing auth() call — do not duplicate it.
    3. In the return JSX, add `{!pledgeAccepted && <PledgeModal />}` as the FIRST child inside QueryProvider, before EmailVerificationBanner. This ensures the modal renders above all other content.

    The existing session.user.id usage in the layout already uses the raw property. Do NOT change how session.user.id is accessed in the existing query — only add the new profiles query using the same id. The auth check (`if (!session?.user?.id) redirect("/auth")`) guarantees session is non-null below that line, so a cast is not needed for the existing user query. For the profiles query, access `session.user.id` directly (it is guaranteed non-null here).
  </action>
  <verify>
    <automated>test -f src/components/auth/PledgeModal.tsx && grep -q "use client" src/components/auth/PledgeModal.tsx && grep -q "PledgeModal" src/app/(app)/layout.tsx && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>PledgeModal.tsx exists as a 'use client' component; uses useMutation to POST /api/pledge/accept; returns null when accepted; overlay is non-dismissible (no X button). layout.tsx imports PledgeModal and profiles, queries pledgeAcceptedAt server-side, conditionally renders PledgeModal. Existing NavShell and EmailVerificationBanner still render. `npx tsc --noEmit` passes with no new errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → POST /api/pledge/accept | Authenticated user submits pledge acceptance |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Spoofing | POST /api/pledge/accept | mitigate | auth() check — returns 401 if no valid session; userId extracted from server-side session object, not request body |
| T-07-02 | Tampering | profiles.pledgeAcceptedAt | accept | Value can only be set to new Date() server-side; client cannot supply a date — no injection surface |
| T-07-03 | Elevation of privilege | layout.tsx pledge gate | mitigate | Gate is server-side; PledgeModal is cosmetic reinforcement only. API routes remain individually auth-gated |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan — uses existing @tanstack/react-query already in package.json |
</threat_model>

<verification>
- `npx tsc --noEmit` passes with no new errors after all three tasks.
- `npx drizzle-kit generate` produced a migration file containing both pledge_accepted_at and spotting_tips.
- Migration applied: DATABASE_URL=$DATABASE_URL_UNPOOLED npx drizzle-kit migrate ran without errors.
- Manual: load any /app/* route as a user without a pledge — full-screen modal appears, no other content visible, background blurred. Click "I Accept the Pledge" — modal disappears without reload, app content becomes visible. Refresh — no modal.
</verification>

<success_criteria>
- schema.ts: profiles has pledgeAcceptedAt (nullable timestamp), species has spottingTips (nullable text).
- POST /api/pledge/accept: 401 for unauthenticated, stamps pledgeAcceptedAt = new Date() and returns { accepted: true } for authenticated.
- PledgeModal: full-screen overlay, pledge text verbatim per D-06, single non-dismissible accept button, disappears on success without page reload.
- AppLayout: reads pledgeAcceptedAt server-side, renders PledgeModal when null, renders nothing extra when non-null.
</success_criteria>

<output>
Create `.planning/phases/07-responsible-spotting-ux/07-01-SUMMARY.md` when done.
</output>
