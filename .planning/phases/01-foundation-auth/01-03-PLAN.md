---
phase: 01-foundation-auth
plan: 03
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - src/components/NavShell.tsx
  - src/app/(app)/layout.tsx
  - src/app/(app)/beastiary/page.tsx
  - src/app/(app)/discover/page.tsx
  - src/components/EmailVerificationBanner.tsx
  - src/app/(app)/home/page.tsx
  - e2e/home.spec.ts
  - e2e/cross-device.spec.ts
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03

must_haves:
  truths:
    - "The authenticated layout renders a bottom tab bar on mobile and a sidebar on desktop with Home, Beastiary, Discover, Profile tabs (D-06)"
    - "All four nav tabs are tappable and route to their respective stub pages (D-06)"
    - "An unverified user (emailVerified=null) sees a non-blocking dismissible email verification banner (D-07)"
    - "The beastiary and discover stubs render Scout-appropriate placeholder content — not blank pages (D-05)"
    - "Unauthenticated access to all four app routes is blocked by middleware and redirects to /auth (AUTH-03)"
    - "e2e/home.spec.ts passes: authenticated /home renders; e2e/cross-device.spec.ts passes: sign-in on second context reaches /home with same user data"
  artifacts:
    - path: "src/components/NavShell.tsx"
      provides: "Responsive nav shell — bottom tab bar (mobile) + sidebar (desktop) with four Scout nav items (D-06)"
      contains: "Beastiary"
    - path: "src/components/EmailVerificationBanner.tsx"
      provides: "Non-blocking email verification prompt (D-07)"
      contains: "emailVerified"
    - path: "src/app/(app)/beastiary/page.tsx"
      provides: "Beastiary stub screen with Scout-appropriate placeholder (D-05)"
      contains: "beastiary"
    - path: "src/app/(app)/discover/page.tsx"
      provides: "Discover stub screen with Scout-appropriate placeholder (D-05)"
      contains: "discover"
    - path: "e2e/home.spec.ts"
      provides: "E2E: authenticated home renders"
      contains: "home"
    - path: "e2e/cross-device.spec.ts"
      provides: "E2E: cross-device login (AUTH-03 verification)"
      contains: "cross"
  key_links:
    - from: "src/app/(app)/layout.tsx"
      to: "src/components/NavShell.tsx"
      via: "NavShell rendered inside authenticated layout wrapping {children}"
      pattern: "NavShell"
    - from: "src/app/(app)/layout.tsx"
      to: "src/components/EmailVerificationBanner.tsx"
      via: "emailVerified from session/DB passed to banner"
      pattern: "EmailVerificationBanner"
---

<objective>
Build the full navigation shell (D-06) — responsive bottom tab bar on mobile, sidebar on desktop — with Home, Beastiary, Discover, Profile stubs all routing correctly. Add the non-blocking email verification banner (D-07). Scaffold Beastiary and Discover stub pages with Scout-appropriate placeholder copy. Add the remaining E2E specs (home + cross-device) to complete the AUTH-03 verification story.

Purpose: After this plan, the app has a complete authenticated shell that every future phase can slot features into without nav refactors. AUTH-03 is fully verifiable via the cross-device E2E spec.
Output: NavShell component, EmailVerificationBanner, beastiary/discover stubs, home + cross-device E2E specs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-foundation-auth/01-CONTEXT.md
@.planning/phases/01-foundation-auth/01-RESEARCH.md
@.planning/phases/01-foundation-auth/01-01-SUMMARY.md
</context>

<interfaces>
<!-- Contracts from Plan 01 consumed here -->

From src/auth.ts:
  export const { auth } = NextAuth({ ... })
  session.user.id — string (uuid)
  session.user.email — string

From src/lib/db/schema.ts:
  users.emailVerified — timestamp | null (null = unverified)

From src/app/(app)/layout.tsx:
  Server Component; calls auth(); renders {children}
  This plan updates it to also render NavShell and EmailVerificationBanner
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: NavShell + EmailVerificationBanner + authenticated layout update</name>
  <files>
    src/components/NavShell.tsx,
    src/components/EmailVerificationBanner.tsx,
    src/app/(app)/layout.tsx,
    src/app/(app)/beastiary/page.tsx,
    src/app/(app)/discover/page.tsx
  </files>
  <read_first>
    - .planning/phases/01-foundation-auth/01-CONTEXT.md — D-06 (bottom nav mobile / sidebar desktop, four tabs: Home, Beastiary, Discover, Profile), D-07 (email verification soft prompt — banner, not blocking gate), D-05 (stub screens with Scout-appropriate placeholder), Claude's Discretion (navigation design — standard mobile-first patterns acceptable)
    - .planning/phases/01-foundation-auth/01-CONTEXT.md — "Specific Ideas" (tabs: Home, Beastiary, Discover, Profile; Discover = GPS discovery Phase 3; Beastiary = collection Phase 5)
  </read_first>
  <action>
Create src/components/NavShell.tsx ("use client"):
  - Props: { children: React.ReactNode; currentPath: string }
  - Mobile layout (below md breakpoint): fixed bottom tab bar with four tabs
  - Desktop layout (md and above): fixed left sidebar with four nav items
  - Nav items (icons from lucide-react or simple SVG; use Tailwind for styling):
      Home: icon Home → href "/home"
      Beastiary: icon BookOpen or Layers → href "/beastiary"
      Discover: icon Compass or MapPin → href "/discover"
      Profile: icon User → href "/profile"
  - Active tab highlight: compare href to currentPath; apply green accent (tailwind green-500 or bg-green-500/10 text-green-500) to active item
  - Accessibility: each nav link has aria-label matching the tab name; nav element wraps the tab list
  - Mobile tab bar: fixed bottom-0 left-0 right-0, border-top, background white/slate-900; each tab is a flex column with icon + label text
  - Desktop sidebar: fixed left-0 top-0 bottom-0 w-56, border-right; nav items stacked vertically with icon + label; a "Scout" wordmark at the top
  - Main content area: on desktop, offset left by sidebar width (ml-56); on mobile, add pb-16 to clear the tab bar

Create src/components/EmailVerificationBanner.tsx ("use client"):
  - Props: { emailVerified: Date | null }
  - If emailVerified is not null, render nothing (null)
  - If emailVerified is null, render a dismissible banner (local state: dismissed boolean):
      Text: "Check your inbox — click the link we sent to verify your account."
      Dismiss button (X icon): sets dismissed = true, hides banner
      The banner does NOT block navigation or content (D-07)
      Styling: amber/yellow tint to signal "action recommended" without alarm

Update src/app/(app)/layout.tsx (Server Component):
  - After auth() call, query users table for emailVerified: SELECT emailVerified FROM users WHERE id = session.user.id
  - Get pathname from headers() (Next.js 16: use headers() from "next/headers" and parse x-invoke-path, or pass currentPath via a server-side mechanism — use a wrapper approach: the layout renders a client NavShell wrapper that reads usePathname() for the active tab)
  - Updated structure:
      <EmailVerificationBanner emailVerified={user.emailVerified} />
      <NavShellWrapper>   {/* thin "use client" wrapper that reads usePathname() */}
        <NavShell currentPath={pathname}>
          <main>{children}</main>
        </NavShell>
      </NavShellWrapper>
  - If pathname reading from headers is complex, make NavShell read usePathname() internally (simpler — NavShell is already "use client")

Create src/app/(app)/beastiary/page.tsx (Server Component):
  - Call auth(); redirect to /auth if no session
  - Render Scout-appropriate stub (D-05):
      Heading: "Your Beastiary"
      Subtext: "Your collection lives here. Go spot something to fill your first card."
      A simple silhouette placeholder grid (CSS — 6 grey rounded squares hinting at future cards)

Create src/app/(app)/discover/page.tsx (Server Component):
  - Call auth(); redirect to /auth if no session
  - Render Scout-appropriate stub (D-05):
      Heading: "Discover"
      Subtext: "What's near you? Location-based discovery is coming — your local wildlife is waiting."
      A simple map/compass placeholder illustration (CSS circle with a compass icon or text)
  </action>
  <verify>
    <automated>cd "/Users/ethanvenables/Documents/Claude/Projects/Scout" && npx tsc --noEmit 2>&1 | head -30</automated>
    <automated>cd "/Users/ethanvenables/Documents/Claude/Projects/Scout" && grep -c "Beastiary" src/components/NavShell.tsx</automated>
    <automated>cd "/Users/ethanvenables/Documents/Claude/Projects/Scout" && grep -c "emailVerified" src/components/EmailVerificationBanner.tsx</automated>
    <automated>cd "/Users/ethanvenables/Documents/Claude/Projects/Scout" && grep -c "beastiary" src/app/\(app\)/beastiary/page.tsx</automated>
  </verify>
  <done>
    - NavShell.tsx renders 4 nav items (Home, Beastiary, Discover, Profile) with mobile bottom tab bar and desktop sidebar
    - EmailVerificationBanner.tsx shows dismissible banner when emailVerified is null (D-07); hides when null prop or dismissed
    - src/app/(app)/layout.tsx renders NavShell + EmailVerificationBanner around {children}
    - beastiary/page.tsx and discover/page.tsx render Scout-appropriate stub content (D-05)
    - npx tsc --noEmit exits clean
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: E2E specs — authenticated home + cross-device (AUTH-03)</name>
  <files>
    e2e/home.spec.ts, e2e/cross-device.spec.ts
  </files>
  <read_first>
    - .planning/phases/01-foundation-auth/01-RESEARCH.md — section 7 (Playwright: unique emails per run, E2E test patterns)
    - .planning/phases/01-foundation-auth/01-VALIDATION.md — Per-Task Verification Map: 01-home (authenticated home renders), 01-middleware (unauthenticated redirect)
    - .planning/phases/01-foundation-auth/01-RESEARCH.md — section 10 Integration Thread #6 (cross-device sync: sign in on second browser → same user row)
  </read_first>
  <action>
Create e2e/home.spec.ts:
  - Uses process.env.E2E_EMAIL and E2E_PASSWORD (set in .env.local by the human checkpoint in Plan 01)
  - "authenticated /home renders nav and stub content":
      Sign in via the /auth form using E2E_EMAIL / E2E_PASSWORD
      Expect URL to be /home
      Expect page to contain "Scout" (heading or wordmark)
      Expect page to contain one of the nav tabs: "Beastiary" visible (nav shell rendered)
      Expect no uncaught console errors
  - "nav tabs route to stub pages":
      After signing in, click "Beastiary" in the nav → expect URL to contain "/beastiary"
      Click "Discover" → expect URL to contain "/discover"
      Click "Profile" → expect URL to contain "/profile"
      Click "Home" → expect URL to contain "/home"

Create e2e/cross-device.spec.ts — AUTH-03 cross-device verification:
  - "same account accessible from two browser contexts":
      Use two separate browser contexts (Playwright's browser.newContext()) to simulate two devices
      Context A: sign up with a unique email (Date.now()); land on /home; read session.user.email from page content
      Context B: sign in with the same email + password; land on /home; verify same email appears in page content
      This proves: user data stored in Neon is accessible from a second session (cloud-sync, AUTH-03)
  - "sign out on one context does not affect the other context's ability to sign in":
      Context A signs out (lands on /auth)
      Context B is still signed in (or can sign back in with same credentials)
      Verify: Context B can navigate to /home successfully (session is independent per-context)

  Both test files import from @playwright/test. Use test.describe blocks for grouping.
  Use a single shared helper function signIn(page, email, password) to avoid repetition between auth.spec.ts, home.spec.ts, cross-device.spec.ts — extract to e2e/helpers.ts.
  </action>
  <verify>
    <automated>cd "/Users/ethanvenables/Documents/Claude/Projects/Scout" && npx tsc --noEmit 2>&1 | head -20</automated>
    <automated>cd "/Users/ethanvenables/Documents/Claude/Projects/Scout" && grep -c "newContext" e2e/cross-device.spec.ts</automated>
    <automated>cd "/Users/ethanvenables/Documents/Claude/Projects/Scout" && grep -c "Beastiary" e2e/home.spec.ts</automated>
    <automated>cd "/Users/ethanvenables/Documents/Claude/Projects/Scout" && test -f e2e/helpers.ts && echo "exists"</automated>
  </verify>
  <done>
    - e2e/home.spec.ts verifies: authenticated /home renders nav shell with all four tabs, tabs route correctly
    - e2e/cross-device.spec.ts verifies: same user row accessible from two browser contexts (AUTH-03)
    - e2e/helpers.ts exports signIn(page, email, password) helper used by all spec files
    - npx tsc --noEmit exits clean on all e2e files
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| NavShell (client) → href routing | href values are static constants — no user input reaches href construction |
| EmailVerificationBanner (client) → banner dismiss | Dismiss is local state only — no server call; no trust boundary crossed |
| (app)/layout.tsx → DB query for emailVerified | Scoped to session.user.id — no other user's data exposed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | EmailVerificationBanner — shows inbox exists | accept | Confirmation that email is registered is low-risk for v1; enumeration protection deferred to v2 |
| T-03-02 | Spoofing | cross-device E2E — two browser contexts share credentials | accept | Test-only concern; E2E_EMAIL/E2E_PASSWORD are in .env.local (gitignored); not a production user |
| T-03-03 | Elevation of Privilege | NavShell href targets | accept | hrefs are static string constants; no user input; routing is enforced server-side by middleware anyway |
| T-03-SC | Tampering | npm packages (no new installs beyond Plan 01 packages) | accept | No new npm installs; packages already in package-lock.json from Plan 01 |
</threat_model>

<verification>
After both tasks and with npm run dev running:
1. `npx tsc --noEmit` — exits 0
2. Navigate to /home as authenticated user → bottom tab bar visible on mobile viewport
3. Tap "Beastiary" → /beastiary loads with "Your Beastiary" heading
4. Tap "Discover" → /discover loads with "Discover" heading
5. Tap "Profile" → /profile loads with email and passkey section
6. A freshly-signed-up user sees the email verification banner on /home
7. Click X on banner → banner disappears; rest of page unaffected (D-07)
8. Navigate to /home /beastiary /discover /profile without session → each redirects to /auth
9. E2E: `npx playwright test e2e/home.spec.ts` — tab routing tests pass
10. E2E: `npx playwright test e2e/cross-device.spec.ts` — two-context test passes (AUTH-03 confirmed)
</verification>

<success_criteria>
- AUTH-03 (cross-device): cross-device.spec.ts passes — same user data accessible from two browser contexts
- D-06: NavShell renders all four tabs; all route to correct stub pages
- D-07: Email verification banner shows for unverified users; dismissible; non-blocking
- D-05: Beastiary and discover stubs render Scout-appropriate placeholder copy
- All E2E spec files type-check cleanly
- Full Playwright suite: `npx playwright test` passes (all specs from Plans 01, 02, 03)
</success_criteria>

<output>
Create `.planning/phases/01-foundation-auth/01-03-SUMMARY.md` when done
</output>
