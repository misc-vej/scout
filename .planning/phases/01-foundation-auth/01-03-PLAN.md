---
phase: 1
plan: 03
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - src/components/nav-shell.tsx
  - src/app/(app)/layout.tsx
  - src/app/(app)/beastiary/page.tsx
  - src/app/(app)/discover/page.tsx
  - e2e/nav.spec.ts
autonomous: true
requirements:
  - AUTH-03
must_haves:
  truths:
    - "AUTH-03: Collection is cloud-synced and accessible after signing in on a different device (proven by signing in on two browser sessions)"
    - "D-06: Full navigation shell scaffolded now with Home, Beastiary, Discover, Profile stubs — later phases activate each section"
    - "D-05: Stub screens for all nav sections convey Scout's vibe rather than showing generic placeholders"
  artifacts:
    - path: "src/components/nav-shell.tsx"
      provides: "Bottom nav (mobile) + sidebar (desktop) with active-state highlighting"
      contains: "usePathname"
    - path: "src/app/(app)/layout.tsx"
      provides: "Authenticated layout with NavShell mounted"
      contains: "NavShell"
    - path: "src/app/(app)/beastiary/page.tsx"
      provides: "Beastiary stub page"
      min_lines: 8
    - path: "src/app/(app)/discover/page.tsx"
      provides: "Discover stub page"
      min_lines: 8
  key_links:
    - from: "src/app/(app)/layout.tsx"
      to: "src/components/nav-shell.tsx"
      via: "import and render"
      pattern: "NavShell"
---

<objective>
After this plan, a signed-in user sees a full navigation shell (bottom tab bar on mobile, sidebar on desktop) with stubs for Home, Beastiary, Discover, and Profile — active tab is highlighted, all routes exist, and signing in on a second browser session confirms cloud-synced auth (AUTH-03 cross-device verification).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-auth/01-CONTEXT.md
@.planning/phases/01-foundation-auth/01-RESEARCH.md
@.planning/phases/01-foundation-auth/01-01-SUMMARY.md

<interfaces>
From Plan 01 — src/app/(app)/layout.tsx: currently a placeholder layout; this plan replaces it with the NavShell layout.
Nav items: Home (/home), Beastiary (/beastiary), Discover (/discover), Profile (/profile)
Icons: lucide-react (Home, BookOpen, Compass, User) — already a Next.js project dep or install if missing.
NavShell pattern: RESEARCH.md §6.
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client navigation | Client-side route changes; all protected by middleware (Plan 01) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Elevation | stub routes bypass middleware | medium | mitigate | Middleware matcher covers /beastiary and /discover (set up in Plan 01); verify unauthenticated access redirects |
| T-03-02 | Info Disclosure | nav reveals premium routes | low | accept | All routes are stubs in Phase 1; no sensitive data rendered |
</threat_model>

<tasks>

<task id="03-1" type="auto">
  <name>Task 1: Nav shell component + authenticated layout</name>
  <read_first>
    - .planning/phases/01-foundation-auth/01-RESEARCH.md — §6 nav shell pattern (bottom nav mobile, sidebar desktop, usePathname active state, lucide-react icons)
    - .planning/phases/01-foundation-auth/01-CONTEXT.md — D-06 (four tabs: Home, Beastiary, Discover, Profile)
    - src/app/(app)/layout.tsx — current placeholder from Plan 01 to replace
    - src/app/globals.css — accent colour variables for active state
  </read_first>
  <action>
    Install `lucide-react` if not already in package.json.
    Create `src/components/nav-shell.tsx` per RESEARCH.md §6: a 'use client' component with two sub-layouts rendered with CSS `md:hidden` / `hidden md:flex` respectively. Nav items array: `[{ href: '/home', label: 'Home', icon: Home }, { href: '/beastiary', label: 'Beastiary', icon: BookOpen }, { href: '/discover', label: 'Discover', icon: Compass }, { href: '/profile', label: 'Profile', icon: User }]`. Mobile: fixed bottom nav (`fixed bottom-0 left-0 right-0 z-50 flex h-16 border-t bg-background md:hidden`), each tab a Link with active state via `usePathname() === href` (text-primary) vs text-muted-foreground. Desktop: fixed left sidebar (`fixed left-0 top-0 hidden h-full w-64 flex-col border-r bg-background p-4 md:flex`), show Scout name/logo at top, same nav links as list items with active bg-primary/10.
    Update `src/app/(app)/layout.tsx` to import NavShell: render a flex container (`flex h-dvh flex-col md:flex-row`) with `<main className="flex-1 overflow-y-auto pb-16 md:pb-0 md:pl-64">{children}</main>` and `<NavShell />`.
  </action>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0
    - `src/components/nav-shell.tsx` contains `usePathname` and all four href values (/home, /beastiary, /discover, /profile)
    - `src/app/(app)/layout.tsx` imports and renders `NavShell`
    - `src/components/nav-shell.tsx` contains `md:hidden` (mobile nav) and `hidden md:flex` (desktop sidebar) CSS classes
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && grep -q "usePathname" src/components/nav-shell.tsx && grep -q "NavShell" src/app/\(app\)/layout.tsx</automated>
  </verify>
  <done>Nav shell renders on all authenticated pages; active tab is highlighted; mobile and desktop layouts are distinct.</done>
</task>

<task id="03-2" type="auto">
  <name>Task 2: Beastiary + Discover stub pages + cross-device E2E</name>
  <read_first>
    - .planning/phases/01-foundation-auth/01-CONTEXT.md — D-06 (non-functional stubs, later phases activate), D-05 (stub content should convey Scout's vibe)
    - src/app/(app)/home/page.tsx — stub pattern established in Plan 01 to replicate
  </read_first>
  <action>
    Create `src/app/(app)/beastiary/page.tsx`: Scout-vibe stub content (e.g., "Your beastiary is empty — get out there and find something worth keeping"). No real data.
    Create `src/app/(app)/discover/page.tsx`: Scout-vibe stub content (e.g., "Discovery coming soon — for now, just keep your eyes open"). No real data.
    Create `e2e/nav.spec.ts`: assert that after sign-in, all four nav links are present in the DOM; clicking /beastiary renders the beastiary stub text; clicking /discover renders the discover stub text. Add a cross-device AUTH-03 test: sign in with an existing test account in a second browser context (using `browser.newContext()`) and assert the /home page loads (proving cloud-synced auth works across sessions).
    Ensure middleware matcher in `src/middleware.ts` covers `/beastiary` and `/discover` (should already from Plan 01's path list, but verify).
  </action>
  <acceptance_criteria>
    - `src/app/(app)/beastiary/page.tsx` exists and renders stub content (non-empty, non-generic)
    - `src/app/(app)/discover/page.tsx` exists and renders stub content
    - `npx playwright test nav` passes including the cross-device sign-in test
    - `grep -E "/beastiary|/discover" src/middleware.ts` finds the protected paths
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && npx playwright test nav</automated>
  </verify>
  <done>All four nav routes exist with Scout-vibe stubs; cross-device sign-in E2E passes; AUTH-03 cross-device criterion met.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` exits 0
2. `npx playwright test nav` passes (nav links present, stubs render, cross-device sign-in works)
3. Manual: `npm run dev` -> sign in -> nav shell visible with four tabs (mobile bottom / desktop sidebar) -> active tab highlighted -> click Beastiary / Discover shows stub screens -> sign in on second incognito window with same credentials -> /home loads
</verification>

<success_criteria>
- Nav shell (bottom nav mobile, sidebar desktop) renders across all authenticated routes (D-06)
- All four routes (Home, Beastiary, Discover, Profile) exist and render Scout-vibe stub content
- Active tab is highlighted; inactive tabs are visually distinct
- User signing in on a second browser session reaches /home (AUTH-03 cross-device sync verified)
- Unauthenticated access to /beastiary and /discover redirects to /auth
</success_criteria>

<must_haves>
  <truths>
    - AUTH-03: User's auth state is accessible across devices (proven by second-browser sign-in E2E)
    - D-06: Full nav shell scaffolded now — Home, Beastiary, Discover, Profile stubs all route-accessible
  </truths>
</must_haves>

<output>
Create `.planning/phases/01-foundation-auth/01-03-SUMMARY.md` when done
</output>
