---
phase: 08-brand-ui-polish
plan: 03
type: execute
wave: 2
depends_on:
  - 08-01
  - 08-02
files_modified:
  - src/app/(app)/beastiary/page.tsx
  - src/components/beastiary/BeastiaryCard.tsx
autonomous: true
requirements:
  - BRAND-03

must_haves:
  truths:
    - "Logbook page has Collection and Not Found tabs (no 'All' tab)"
    - "Rarity filter pill row shows: All, Common, Uncommon, Rare, V.Rare, Legendary, Mythic"
    - "Collected cards show AnimalIcon at 54% size + Grain overlay + tier border + glow on Rare+"
    - "Locked cards show '?' in Syne 800 ~46px faded to tierColor18 + 'Not found' italic label"
    - "Detail bottom sheet slides up on collected card tap with 180px art zone, tier border, personality + habitat chips, stats row"
    - "Card hover: scale(1.04) with 0.15s ease transition"
  artifacts:
    - path: "src/app/(app)/beastiary/page.tsx"
      provides: "Logbook page with Collection/Not Found tabs and rarity filter"
      contains: "Collection"
    - path: "src/components/beastiary/BeastiaryCard.tsx"
      provides: "CollectedCard and LockedCard with SVG silhouettes and glow"
      contains: "AnimalIcon"
  key_links:
    - from: "src/app/(app)/beastiary/page.tsx"
      to: "src/components/beastiary/BeastiaryCard.tsx"
      via: "renders CollectedCard / LockedCard"
      pattern: "BeastiaryCard"
    - from: "src/components/beastiary/BeastiaryCard.tsx"
      to: "src/components/shared/AnimalIcon.tsx"
      via: "import AnimalIcon"
      pattern: "AnimalIcon"
    - from: "src/components/beastiary/BeastiaryCard.tsx"
      to: "src/lib/rarity.ts"
      via: "import getRarityConfig"
      pattern: "getRarityConfig"
---

<objective>
Redesign the Beastiary (Logbook) screen to match the dark Forest Night prototype from ScoutBeastiary.jsx. Replace the taxonomy-grouped list with a Collection/Not Found tab structure, rarity filter pills, a 3-column card grid with SVG silhouette art zones, and a detail bottom sheet.

Purpose: Delivers the Logbook visual design per BRAND-03. Depends on Wave 1 primitives (AnimalIcon, getRarityConfig, CSS variables, Syne/Outfit fonts).
Output: Redesigned beastiary/page.tsx (server component with client tab/filter state) and BeastiaryCard.tsx (replaces existing component).
</objective>

<execution_context>
@/Users/ethanvenables/Documents/Claude/Projects/Scout/.planning/phases/08-brand-ui-polish/08-CONTEXT.md
</execution_context>

<context>
@/Users/ethanvenables/Documents/Claude/Projects/Scout/.planning/ROADMAP.md
@/Users/ethanvenables/Documents/Claude/Projects/Scout/src/app/(app)/beastiary/page.tsx
@/Users/ethanvenables/Documents/Claude/Projects/Scout/src/components/beastiary/BeastiaryCard.tsx
@/Users/ethanvenables/Documents/Claude/Projects/Scout/src/components/beastiary/PersonalityPicker.tsx
@/Users/ethanvenables/Documents/Claude/Projects/Scout/.planning/phases/08-brand-ui-polish/08-02-SUMMARY.md

Design source of truth:
- /tmp/scout_design/design_handoff_scout/ScoutBeastiary.jsx (full Logbook prototype — CollectedCard, LockedCard, DetailPanel components)
- /tmp/scout_design/design_handoff_scout/README.md (card anatomy, chip colour specs)

<interfaces>
<!-- From 08-02 primitives -->

From src/components/shared/AnimalIcon.tsx:
```typescript
export function AnimalIcon({ type, color }: { type: string; color: string }): JSX.Element
```

From src/lib/rarity.ts:
```typescript
export interface RarityConfig {
  borderColor: string;    // e.g. "#c8922a"
  artBg: string;          // e.g. "#1c1402"
  glowAnimation: string;  // e.g. "gold-glow 2.8s ease-in-out infinite" or "none"
  label: string;          // e.g. "Rare"
}
export function getRarityConfig(tier: string): RarityConfig
```

From src/app/(app)/beastiary/page.tsx (current data shape passed to BeastiaryCard):
```typescript
// species columns available from DB:
species.id, species.commonName, species.scientificName, species.rarityTier,
species.speciesType, species.funFact, species.conservationStatus,
species.spottingTips, species.sensitivityLevel

// collections columns:
collections.sightingCount, collections.personalityTrait, collections.isShiny,
collections.firstSightedAt
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: BeastiaryCard.tsx — CollectedCard, LockedCard, DetailPanel</name>
  <files>src/components/beastiary/BeastiaryCard.tsx</files>
  <read_first>
    - src/components/beastiary/BeastiaryCard.tsx (current implementation — note PersonalityPicker import; it must be preserved)
    - /tmp/scout_design/design_handoff_scout/ScoutBeastiary.jsx (CollectedCard lines 198–226, LockedCard 228–248, DetailPanel 250–303 — use as exact reference)
    - src/lib/rarity.ts (getRarityConfig return shape — available after 08-02)
    - src/components/shared/AnimalIcon.tsx (AnimalIcon props)
    - /Users/ethanvenables/Documents/Claude/Projects/Scout/.planning/phases/08-brand-ui-polish/08-CONTEXT.md (D-08, D-09)
  </read_first>
  <action>
    Replace the contents of BeastiaryCard.tsx entirely. This is a "use client" component (it manages hover state and the detail sheet open/close state via props).

    **Props interface (BeastiaryCardProps):**
    ```
    species: {
      id: string;
      commonName: string;
      scientificName: string;
      rarityTier: string;
      speciesType: string | null;
      funFact: string | null;
      spottingTips: string | null;
      sensitivityLevel: string;
      no: string;            // zero-padded card number, e.g. "#001"
      habitat: string | null; // from taxonomyGroup (passed through)
    };
    sightingCount?: number;
    personalityTrait?: string | null;
    isShiny?: boolean;
    firstSightedAt?: string | null;  // ISO string or null
    onCardTap: (species: BeastiaryCardProps["species"], personalityTrait: string | null) => void;
    ```

    **CollectedCard rendering** (per ScoutBeastiary.jsx CollectedCard + D-08):
    - Outer div: `cursor: pointer`, `borderRadius: 12`, `overflow: hidden`, `position: relative`
    - `border: 2.5px solid <borderColor>` where borderColor = isShiny ? "#fff" : rarityConfig.borderColor
    - `background: rarityConfig.artBg`
    - `animation: isShiny ? "white-glow 2s ease-in-out infinite" : (rarityConfig.glowAnimation)`
    - `aspectRatio: "5/7"`, display: flex, flexDirection: column
    - Hover: `transform: scale(1.04)` with `transition: transform .15s ease` — manage with React `useState(false)` for isHovered
    - Art zone (flex: 1, display: flex, alignItems: center, justifyContent: center, position: relative, overflow: hidden, minHeight: 0):
      - `<AnimalIcon type={species.speciesType ?? "bird"} color={isShiny ? "rgba(255,255,255,.45)" : rarityConfig.borderColor + "77"} />`
      - Grain overlay div: `className="grain"` — this uses the `.grain` CSS class from globals.css (position absolute, inset 0, pointer-events none, z-index 1)
      - Shiny shimmer overlay when isShiny: position absolute, inset 0, `background: linear-gradient(135deg,rgba(255,255,255,0) 30%,rgba(255,255,255,.05) 50%,rgba(255,255,255,0) 70%)`, z-index 2
    - Info strip (padding: "5px 7px 8px", flexShrink: 0, zIndex: 3, position: relative):
      - Tier label: Outfit 9px 600, uppercase, letter-spacing .06em, color: isShiny ? "#fff" : rarityConfig.borderColor; truncated; text: isShiny ? "✦ Shiny" : rarityConfig.label
      - Species name: Syne 10.5px 700, Mist (#e8f0e4), uppercase, letter-spacing .02em, lineHeight 1.15, marginTop 2; truncated
      - Personality chip (only if personalityTrait): background rgba(184,120,232,.15), border 1px solid rgba(184,120,232,.22), Outfit 7.5px, color #b878e8, weight 600, padding 2px 6px, borderRadius 20, marginTop 3, display inline-block

    **LockedCard rendering** (per ScoutBeastiary.jsx LockedCard + D-08):
    - Outer div: same structure but `background: #080e0a`; no glow animation
    - Art zone: `?` in Syne 800 46px, color `${rarityConfig.borderColor}18`, lineHeight 1, position relative, zIndex 2; plus Grain overlay at opacity 0.4
    - Info strip: Tier label (Outfit 9px 600, rarityConfig.borderColor, uppercase, opacity .5); "Not found" in Outfit 9px, rgba(232,240,228,.1), fontStyle italic

    **DetailPanel** (per D-09 and ScoutBeastiary.jsx DetailPanel lines 250–303):
    This is rendered by the parent page when a card is tapped — the BeastiaryCard component calls `onCardTap` on click. Do not embed the full DetailPanel inside BeastiaryCard. Instead, export a separate `DetailPanel` named export from the same file.

    DetailPanel props: `{ species: BeastiaryCardProps["species"]; personalityTrait: string | null; firstSightedAt: string | null; sightingCount: number; onClose: () => void; }`

    DetailPanel renders (per D-09):
    - Fixed overlay: `rgba(0,0,0,.8)`, zIndex 200, flex, alignItems flex-end, justifyContent center; clicking overlay calls onClose
    - Sheet: width 100%, maxWidth 390, background #0d1c12, borderRadius "20px 20px 0 0", border `3px solid ${borderColor}`, borderBottom none; glow animation on rare+
    - Drag handle: 36×4px pill, background rgba(255,255,255,.1), borderRadius 2, centered, padding 10px 0 0
    - Art zone: height 180, background `linear-gradient(160deg,${rarityConfig.artBg},#08100a)`, margin 8px 14px 0, borderRadius 12, overflow hidden, relative; contains AnimalIcon at large size (wrapper div 100%×100%); Grain overlay; tier badge top-left (Outfit 9px 700 uppercase, border 1px solid borderColor, borderRadius 20, padding 3px 9px); card number top-right (Outfit 9px, rgba(232,240,228,.25))
    - Name: Syne 800 24px #e8f0e4 uppercase letter-spacing .05em lineHeight 1
    - Latin: Outfit italic 11px #2e5a3a marginTop 3
    - Chips (display flex, gap 6, marginTop 10, flexWrap wrap):
      - Personality chip if set: background rgba(184,120,232,.15), border rgba(184,120,232,.25), Outfit 10px 600, color #b878e8, padding 5px 12px, borderRadius 20
      - Habitat chip if set: background rgba(114,204,74,.12), border rgba(114,204,74,.2), Outfit 10px, color #72cc4a, padding 5px 12px, borderRadius 20
    - Stats row (borderTop 1px solid rgba(255,255,255,.06), paddingTop 12, marginTop 14, display flex, justifyContent space-around):
      - sightingCount × "spotted", firstSightedAt formatted as "Jun 25" (Syne 18px 800 #e8f0e4 lineHeight 1 + Outfit 8px #2e5a3a uppercase letter-spacing .08em marginTop 3)

    Keep PersonalityPicker import — but do not render it inside this redesigned BeastiaryCard (the personality is now set via the log modal in the Nearby flow; the existing PersonalityPicker remains intact but is no longer rendered here per the new design).

    Import `AnimalIcon` from `@/components/shared/AnimalIcon`. Import `getRarityConfig` from `@/lib/rarity`.

    Do NOT use Tailwind classes for colours or typography in this component — use inline style={{}} objects matching the prototype exactly. Tailwind flex/grid layout utilities are allowed for structural layout only.
  </action>
  <verify>
    <automated>cd /Users/ethanvenables/Documents/Claude/Projects/Scout && npx tsc --noEmit 2>&1 | grep "BeastiaryCard\|AnimalIcon\|rarity" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "AnimalIcon" src/components/beastiary/BeastiaryCard.tsx` outputs at least 2 (import + usage)
    - `grep -c "getRarityConfig" src/components/beastiary/BeastiaryCard.tsx` outputs at least 2 (import + usage)
    - `grep -c "DetailPanel" src/components/beastiary/BeastiaryCard.tsx` outputs at least 2 (definition + export)
    - `grep -c "aspectRatio" src/components/beastiary/BeastiaryCard.tsx` outputs at least 1 ("5/7")
    - `grep -c "white-glow\|gold-glow\|orange-glow\|purple-glow\|silver-glow" src/components/beastiary/BeastiaryCard.tsx` outputs at least 1
    - `grep -c "onCardTap" src/components/beastiary/BeastiaryCard.tsx` outputs at least 2 (prop definition + usage)
    - `grep -c "\"use client\"" src/components/beastiary/BeastiaryCard.tsx` outputs 1
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>BeastiaryCard.tsx exports BeastiaryCard (CollectedCard/LockedCard) and DetailPanel. Uses AnimalIcon + getRarityConfig. Inline styles match prototype. tsc exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: beastiary/page.tsx — Logbook redesign with tabs and rarity filter</name>
  <files>src/app/(app)/beastiary/page.tsx</files>
  <read_first>
    - src/app/(app)/beastiary/page.tsx (current server component — note DB query structure to preserve)
    - /tmp/scout_design/design_handoff_scout/ScoutBeastiary.jsx (ScoutBeastiary component lines 305–397 — tab logic, rarity filter, grid rendering)
    - /Users/ethanvenables/Documents/Claude/Projects/Scout/.planning/phases/08-brand-ui-polish/08-CONTEXT.md (D-08, D-09, D-12)
    - src/components/beastiary/BeastiaryCard.tsx (new BeastiaryCard props — especially `no`, `habitat`, `onCardTap` fields)
  </read_first>
  <action>
    The page remains a server component that fetches data. Extract the interactive tab/filter/detail-sheet UI into a `BeastiaryClient` client component within the same file (or as a sibling file `src/components/beastiary/BeastiaryClient.tsx`).

    **Server component (BeastiaryPage) changes:**
    - Keep the existing `auth()` gate and DB queries for allSpecies and userCollections
    - Add `speciesType: species.speciesType` to the `.select()` call in the allSpecies query
    - Add `firstSightedAt: collections.firstSightedAt` to the userCollections select
    - Pass to BeastiaryClient:
      - `allSpecies` array (includes speciesType)
      - `collectedMap` (object or Map) with speciesId → { sightingCount, personalityTrait, isShiny, firstSightedAt }
      - `totalCollected` and `totalSpecies`
    - The card `no` (zero-padded number): derive from the species' position in the `allSpecies` array (sorted by id ASC). Format: `"#" + String(index + 1).padStart(3, "0")`. Pass as a field in the species object.
    - The species `habitat` field: pass `taxonomyGroup` from DB as the habitat value (no separate habitat field exists; taxonomyGroup is the closest approximation).

    **BeastiaryClient layout** (matching ScoutBeastiary.jsx):
    - Container: minHeight 100vh, background #0a1410, fontFamily Outfit, display flex, flexDirection column, maxWidth 390, margin 0 auto

    - **Nav bar** (per D-12): sticky, top 0, zIndex 50, height 56, background linear-gradient(180deg,#0d1c12,#091410), borderBottom 1px solid rgba(114,204,74,.1), padding 0 20px, display flex, alignItems center, justifyContent space-between:
      - Left: "SCOUT" in Syne 800 17px #e8f0e4 uppercase letter-spacing .07em
      - Right: collected/total pill in Signal green (background rgba(114,204,74,.12), border rgba(114,204,74,.2), borderRadius 20, padding 4px 10px, Outfit 11px 600 #72cc4a)

    - **Tab bar** (Collection | Not Found): background #091410, padding 12px 16px 0, display flex, gap 6. Active tab: background #72cc4a, borderRadius 10, padding 10px 0, Syne 13px 700, color #0d1c12, no border. Inactive: background rgba(114,204,74,.07), border 1px solid rgba(114,204,74,.12), Syne 13px 700, color #2e5a3a.

    - **Rarity filter pills** (horizontal scroll, no wrap): background #091410, padding 10px 16px 12px, display flex, gap 5, overflowX auto, scrollbarWidth none. Filter values: ["all","common","uncommon","rare","super_rare","legendary","mythic"] Labels: "All", "Common", "Uncommon", "Rare", "V.Rare", "Legendary", "Mythic". Active pill: background `${color}22`, border `1px solid ${color}`, color: filter-color. Inactive: background transparent, border 1px solid rgba(255,255,255,.06), color #2e5a3a. Filter color for "all": #3a6a48; others use getRarityConfig().borderColor.

    - **Card grid**: flex 1, overflowY auto, padding 10px 14px 100px. Grid: display grid, gridTemplateColumns repeat(3,1fr), gap 8. Render BeastiaryCard for each species in the filtered list. Pass `onCardTap` that sets `selectedSpecies + selectedPersonality` state.

    - **Bottom nav** (per D-13): fixed bottom, left 50%, translateX(-50%), width 100%, maxWidth 390, background linear-gradient(to top,#0a1410 65%,rgba(10,20,16,0)), padding 8px 0 20px, display flex, justifyContent space-around, zIndex 40. Two items:
      - Nearby: SVG location pin icon (22×22, from ScoutNearby.jsx nav inactive), label "Nearby" Outfit 9px 600 #2e5a3a; links to /discover
      - Logbook: SVG 4-square grid icon (active), label "Logbook" Outfit 9px 600 #72cc4a + 4×4 Signal green dot below; current page

    - **DetailPanel**: rendered via `{selected && <DetailPanel ... />}` — import from BeastiaryCard.tsx. Show when a collected card is tapped; hide on overlay click.

    Empty state (when filtered list is empty): centered text "Nothing here" Syne 15px 700 #1e3828 uppercase + "Try a different filter." Outfit 12px #162218.

    State managed in BeastiaryClient:
    - `tab: "collection" | "lost"` (default: "collection")
    - `rarityFilter: string` (default: "all")
    - `selected: SpeciesRow | null` and `selectedPersonality: string | null`

    When tab changes, reset rarityFilter to "all".
  </action>
  <verify>
    <automated>cd /Users/ethanvenables/Documents/Claude/Projects/Scout && npx tsc --noEmit 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Collection" src/app/\(app\)/beastiary/page.tsx` outputs at least 1 OR exists in BeastiaryClient.tsx
    - `grep -c "rarityFilter" src/app/\(app\)/beastiary/page.tsx` outputs at least 1 OR exists in BeastiaryClient.tsx
    - `grep -c "DetailPanel" src/app/\(app\)/beastiary/page.tsx` outputs at least 1 OR exists in BeastiaryClient.tsx
    - `grep -c "speciesType" src/app/\(app\)/beastiary/page.tsx` outputs at least 1 (in the DB select)
    - `grep -c "firstSightedAt" src/app/\(app\)/beastiary/page.tsx` outputs at least 1
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Logbook page has Collection/Not Found tabs, rarity filter pills, 3-column grid, and DetailPanel bottom sheet. Nav bar shows SCOUT wordmark + progress pill. Bottom nav with Nearby/Logbook SVG icons. tsc exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB → server component | Species and collections data fetched server-side |
| Client component | BeastiaryClient holds UI state; no sensitive data exposed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03 | Information Disclosure | BeastiaryPage server component | accept | Existing auth() gate maintained; no new server-side data exposed |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | No new packages |
</threat_model>

<verification>
- Logbook renders with Collection/Not Found tabs; toggling tabs switches the grid content
- Rarity filter pills filter the visible cards; selecting a rarity hides non-matching cards
- Tapping a collected card opens the DetailPanel bottom sheet
- AnimalIcon silhouettes visible in card art zones (not letter initials)
- Glow animations visible on Rare+ cards
- `npx tsc --noEmit` exits 0
</verification>

<success_criteria>
- Logbook screen matches ScoutBeastiary.jsx prototype: Forest Night bg, tab bar, rarity filters, SVG silhouettes, glow animations, personality chips
- Locked cards show "?" not letter initial
- Detail sheet opens on collected card tap with art zone, chips, stats
- tsc exits 0
</success_criteria>

<output>
Create `/Users/ethanvenables/Documents/Claude/Projects/Scout/.planning/phases/08-brand-ui-polish/08-03-SUMMARY.md` when done.
</output>
