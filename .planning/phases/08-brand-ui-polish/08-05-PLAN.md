---
phase: 08-brand-ui-polish
plan: 05
type: execute
wave: 3
depends_on:
  - 08-03
  - 08-04
files_modified:
  - src/components/NavShell.tsx
  - src/app/(app)/layout.tsx
  - src/components/auth/PledgeModal.tsx
autonomous: true
requirements:
  - BRAND-02
  - BRAND-05

must_haves:
  truths:
    - "NavShell renders the Forest Night top bar (SCOUT wordmark, Syne 800, #e8f0e4) and a Forest Night bottom tab bar with SVG Nearby and Logbook icons"
    - "Active tab has Signal green (#72cc4a) icon + green dot indicator; inactive tab has #2e5a3a icons"
    - "PledgeModal uses Forest Night styling: background #0d1c12, Syne 800 title, Outfit body, Signal CTA"
    - "npx tsc --noEmit exits 0 across the entire project"
  artifacts:
    - path: "src/components/NavShell.tsx"
      provides: "Forest Night nav with SVG Nearby/Logbook icons"
      contains: "SCOUT"
    - path: "src/components/auth/PledgeModal.tsx"
      provides: "Forest Night pledge modal"
      contains: "#0d1c12"
  key_links:
    - from: "src/components/NavShell.tsx"
      to: "src/app/(app)/beastiary/page.tsx"
      via: "href /beastiary"
      pattern: "beastiary"
    - from: "src/components/NavShell.tsx"
      to: "src/app/(app)/discover/page.tsx"
      via: "href /discover"
      pattern: "discover"
---

<objective>
Update global nav chrome (NavShell) with the Forest Night design system: SCOUT wordmark, SVG Nearby/Logbook bottom tab icons, active dot indicator. Polish the PledgeModal to match Forest Night. Verify the entire project compiles cleanly.

Purpose: Final Wave 3 plan — wires together all the visual changes and enforces tsc correctness across the whole codebase.
Output: NavShell.tsx redesigned with Forest Night + SVG icons; PledgeModal.tsx polished; full project tsc clean.
</objective>

<execution_context>
@/Users/ethanvenables/Documents/Claude/Projects/Scout/.planning/phases/08-brand-ui-polish/08-CONTEXT.md
</execution_context>

<context>
@/Users/ethanvenables/Documents/Claude/Projects/Scout/.planning/ROADMAP.md
@/Users/ethanvenables/Documents/Claude/Projects/Scout/src/components/NavShell.tsx
@/Users/ethanvenables/Documents/Claude/Projects/Scout/src/app/(app)/layout.tsx
@/Users/ethanvenables/Documents/Claude/Projects/Scout/src/components/auth/PledgeModal.tsx

Design source of truth:
- /tmp/scout_design/design_handoff_scout/ScoutBeastiary.jsx (bottom nav, nav bar — lines 319–392)
- /tmp/scout_design/design_handoff_scout/ScoutNearby.jsx (top nav bar lines 277–289, bottom nav lines 309–322)
- /tmp/scout_design/design_handoff_scout/README.md (bottom nav design spec)
- /Users/ethanvenables/Documents/Claude/Projects/Scout/.planning/phases/08-brand-ui-polish/08-CONTEXT.md (D-12, D-13, D-14)

Prior phase summaries (read if available):
- /Users/ethanvenables/Documents/Claude/Projects/Scout/.planning/phases/08-brand-ui-polish/08-03-SUMMARY.md
- /Users/ethanvenables/Documents/Claude/Projects/Scout/.planning/phases/08-brand-ui-polish/08-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: NavShell.tsx — Forest Night nav with SVG icons and active dot</name>
  <files>src/components/NavShell.tsx</files>
  <read_first>
    - src/components/NavShell.tsx (current — Lucide icons, light sidebar, mobile tab bar)
    - /tmp/scout_design/design_handoff_scout/ScoutBeastiary.jsx (IconNearby lines 176–183, IconLogbook 185–196, bottom nav 380–391)
    - /tmp/scout_design/design_handoff_scout/ScoutNearby.jsx (top nav 277–289, bottom nav 309–323)
    - /Users/ethanvenables/Documents/Claude/Projects/Scout/.planning/phases/08-brand-ui-polish/08-CONTEXT.md (D-12, D-13)
  </read_first>
  <action>
    Replace the current NavShell implementation. The new design matches the Scout dark prototype with a focused 2-tab navigation (Nearby + Logbook only — no Home or Profile tabs in the mobile bottom bar).

    **Keep `"use client"` and `usePathname` import.** Remove the lucide-react icon imports.

    Define two inline SVG icon components in the same file:

    `IconNearby({ active }: { active: boolean })` — copy EXACTLY from ScoutBeastiary.jsx lines 176–183:
    - SVG 22×22, viewBox "0 0 24 24", fill "none"
    - stroke color: active ? "#72cc4a" : "#2e5a3a", strokeWidth "1.7"
    - Contains circle (cx=12 cy=10 r=3.5) and location pin path

    `IconLogbook({ active }: { active: boolean })` — copy EXACTLY from ScoutBeastiary.jsx lines 185–196:
    - SVG 22×22, viewBox "0 0 24 24", fill "none"
    - stroke color: active ? "#72cc4a" : "#2e5a3a", strokeWidth "1.7"
    - Contains four rect elements (3×3+8+8, 13×3+8+8, 3×13+8+8, 13×13+8+8) with rx=1.5

    The nav items are now only two: `{ label: "Nearby", href: "/discover", Icon: IconNearby }` and `{ label: "Logbook", href: "/beastiary", Icon: IconLogbook }`.

    **Mobile layout (primary — max-width 390 container):**
    Full-height flex column container: `style={{ minHeight: "100vh", background: "#0a1410", display: "flex", flexDirection: "column", maxWidth: 390, margin: "0 auto" }}`

    **Top nav bar** (per D-12): sticky top 0, zIndex 50, height 56, background `linear-gradient(180deg,#0d1c12,#091410)`, borderBottom `1px solid rgba(114,204,74,.1)`, padding `0 20px`, display flex, alignItems center, justifyContent space-between:
    - Left: `<span style={{ fontFamily:"Syne,sans-serif", fontSize:17, fontWeight:800, color:"#e8f0e4", letterSpacing:".07em", textTransform:"uppercase" }}>SCOUT</span>`
    - Right: nothing (progress counter is rendered per-page in the Logbook page nav; this global nav has no counter)

    **Main content area**: `<main style={{ flex: 1, paddingBottom: 80 }}>{children}</main>`

    **Bottom tab bar** (per D-13): fixed bottom, left 50%, translateX(-50%), width 100%, maxWidth 390, background `linear-gradient(to top,#0a1410 65%,rgba(10,20,16,0))`, padding `8px 0 20px`, display flex, justifyContent space-around, zIndex 40.

    For each nav item:
    ```
    <Link href={href} style={{ textAlign: "center", cursor: "pointer", minWidth: 80, padding: "4px 0" }}>
      <div style={{ display: "flex", justifyContent: "center" }}>
        <Icon active={active} />
      </div>
      <div style={{ fontFamily:"Outfit,sans-serif", fontSize:9, fontWeight:600, color: active?"#72cc4a":"#2e5a3a", marginTop:4, letterSpacing:".06em", textTransform:"uppercase" }}>
        {label}
      </div>
      {active && <div style={{ width:4, height:4, borderRadius:"50%", background:"#72cc4a", margin:"3px auto 0" }} />}
    </Link>
    ```
    Active is determined by `pathname.startsWith(href)`.

    **Desktop sidebar**: keep it (md:flex md:flex-col md:fixed md:inset-y-0 md:left-0 md:w-56) but update it to Forest Night styling:
    - Background: #0d1c12; border-right: 1px solid rgba(114,204,74,.1)
    - SCOUT wordmark: Syne 800, #e8f0e4, 20px
    - Nav items: flex, gap 3, px 3, py 2.5, rounded-lg; active: background rgba(114,204,74,.1), color #72cc4a; inactive: color #2e5a3a hover:color:#e8f0e4

    Remove the `Home` and `Profile` nav items — keep Nearby (/discover) and Logbook (/beastiary) only.

    Do not remove the `md:ml-56` offset on main — keep it for desktop sidebar layout.
  </action>
  <verify>
    <automated>cd /Users/ethanvenables/Documents/Claude/Projects/Scout && npx tsc --noEmit 2>&1 | grep "NavShell" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "SCOUT" src/components/NavShell.tsx` outputs at least 1 (wordmark)
    - `grep -c "IconNearby\|IconLogbook" src/components/NavShell.tsx` outputs at least 4 (definitions + usages)
    - `grep -c "#72cc4a" src/components/NavShell.tsx` outputs at least 3 (active states)
    - `grep -c "#2e5a3a" src/components/NavShell.tsx` outputs at least 2 (inactive states)
    - `grep -c "lucide-react" src/components/NavShell.tsx` outputs 0 (old icon library removed)
    - `grep -c "Home\|Profile" src/components/NavShell.tsx` outputs 0 (removed nav items)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>NavShell uses Forest Night styling, SVG Nearby/Logbook icons (copied from prototype), active dot, SCOUT wordmark. Lucide removed. tsc exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: PledgeModal.tsx + full project tsc check</name>
  <files>src/components/auth/PledgeModal.tsx</files>
  <read_first>
    - src/components/auth/PledgeModal.tsx (current — gray-900 bg, white/10 border, plain text)
    - /Users/ethanvenables/Documents/Claude/Projects/Scout/.planning/phases/08-brand-ui-polish/08-CONTEXT.md (D-14 — exact styling values)
  </read_first>
  <action>
    Update PledgeModal.tsx to use Forest Night styling (per D-14). The component logic and mutation call (`POST /api/pledge/accept`) must not change. Only the visual presentation changes.

    **Overlay**: keep `fixed inset-0 z-50 flex items-center justify-center p-4`; update `bg-black/80 backdrop-blur-sm` to add `background: rgba(0,0,0,.85)`.

    **Modal container**: replace `rounded-2xl border border-white/10 bg-gray-900 p-8 shadow-2xl` with inline styles:
    - `background: #0d1c12`
    - `border: 1px solid rgba(114,204,74,.15)`
    - `borderRadius: 18`
    - `padding: 32px`
    - `maxWidth: 400, width: "100%"`
    - `boxShadow: "0 20px 60px rgba(0,0,0,.6)"`

    **Title** (`h2`): remove `text-xl font-bold text-white`; add inline:
    - `fontFamily: "Syne, sans-serif"`, `fontSize: 20`, `fontWeight: 800`, `color: "#e8f0e4"`, `textTransform: "uppercase"`, `letterSpacing: ".04em"`, `marginBottom: 4`

    **Subtitle** (`p.mb-6`): remove `text-sm text-gray-400`; add inline:
    - `fontFamily: "Outfit, sans-serif"`, `fontSize: 13`, `color: "#2e5a3a"`, `marginBottom: 24`, `lineHeight: 1.6`

    **Pledge text box** (the div with pledge text): replace `rounded-lg border border-white/10 bg-white/5 p-4 text-sm text-gray-300 leading-relaxed` with inline:
    - `background: rgba(114,204,74,.06)`, `border: "1px solid rgba(114,204,74,.1)"`, `borderRadius: 10`, `padding: "12px 14px"`, `marginBottom: 24`
    - Text: `fontFamily: "Outfit, sans-serif"`, `fontSize: 13`, `color: "#3a6040"`, `lineHeight: 1.7`

    **CTA button**: replace `rounded-xl bg-green-600 py-3 font-semibold text-white hover:bg-green-500 disabled:opacity-50 transition-colors` with inline:
    - Normal: `background: "#72cc4a"`, `color: "#0d1c12"`, `borderRadius: 14`, `padding: "14px 0"`, `width: "100%"`, `fontFamily: "Syne, sans-serif"`, `fontSize: 15`, `fontWeight: 800`, `textTransform: "uppercase"`, `letterSpacing: ".05em"`, `border: "none"`, `cursor: "pointer"`
    - Pending (disabled): `opacity: 0.5, cursor: "default"`
    - Button text: mutation.isPending ? "Saving…" : "I Accept the Pledge" (unchanged)

    After updating PledgeModal.tsx, run the full project TypeScript check:
    ```
    cd /Users/ethanvenables/Documents/Claude/Projects/Scout && npx tsc --noEmit
    ```
    Fix ANY type errors found across the entire project (not just the files modified in Phase 8). Common issues to check:
    - BeastiaryCard.tsx: all props passed from page.tsx exist in the updated props interface
    - SpeciesCard.tsx (discover): SpeciesResult type imports match updated type
    - Any implicit `any` in new inline style objects (add `React.CSSProperties` cast if needed)
    - discovery.ts: SpeciesResult is consumed by DiscoverClient — ensure no breakage
  </action>
  <verify>
    <automated>cd /Users/ethanvenables/Documents/Claude/Projects/Scout && npx tsc --noEmit 2>&1; echo "EXIT:$?"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "#0d1c12" src/components/auth/PledgeModal.tsx` outputs at least 1 (modal background)
    - `grep -c "rgba(114,204,74,.15)" src/components/auth/PledgeModal.tsx` outputs 1 (modal border)
    - `grep -c "Syne" src/components/auth/PledgeModal.tsx` outputs at least 1 (title font)
    - `grep -c "#72cc4a" src/components/auth/PledgeModal.tsx` outputs at least 1 (CTA background)
    - `grep -c "bg-gray-900" src/components/auth/PledgeModal.tsx` outputs 0 (old Tailwind color removed)
    - `npx tsc --noEmit` exits 0 (the entire project — zero type errors)
  </acceptance_criteria>
  <done>PledgeModal uses Forest Night bg (#0d1c12), Signal green border and CTA, Syne 800 title, Outfit body. Full project npx tsc --noEmit exits 0 with no type errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → /api/pledge/accept | POST; existing auth gate maintained |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-05 | Tampering | PledgeModal mutation | accept | Route unchanged; existing auth() gate on /api/pledge/accept maintained |
| T-08-SC | Tampering | npm installs | accept | No new packages added |
</threat_model>

<verification>
- NavShell: SCOUT wordmark visible in top bar; bottom tab bar shows Nearby (pin icon) and Logbook (grid icon); active tab has green icon + dot; inactive has muted green icons
- PledgeModal: Forest Night background, Syne title, Signal CTA; pledge accept mutation still functional
- `npx tsc --noEmit` exits 0 with no errors across entire project
</verification>

<success_criteria>
- NavShell displays Forest Night styling end-to-end: dark top bar, SVG tab icons, active dot indicator
- PledgeModal visually matches D-14: #0d1c12 bg, rgba(114,204,74,.15) border, Syne 800 title, #72cc4a CTA
- Full project tsc exits 0 — zero type errors remaining
</success_criteria>

<output>
Create `/Users/ethanvenables/Documents/Claude/Projects/Scout/.planning/phases/08-brand-ui-polish/08-05-SUMMARY.md` when done.
</output>
