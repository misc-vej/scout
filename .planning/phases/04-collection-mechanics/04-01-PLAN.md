---
plan: 04-01
title: sightings + collections schema + Drizzle migration
phase: 4
wave: 1
depends_on: []
req_ids: [DISC-02, DISC-03]
branching_strategy: none
autonomous: true
files_modified:
  - src/lib/db/schema.ts
  - drizzle/migrations/ (generated migration SQL)
must_haves:
  truths:
    - D-01: sightings table exists in Neon — append-only, one row per log tap
    - D-01: collections table exists in Neon — unique (userId, speciesId), sightingCount starts at 1
    - D-01: Both tables have cascade deletes on userId and speciesId FKs
    - D-01: Unique index collections_user_species_idx enforces one card per (user, species)
  artifacts:
    - src/lib/db/schema.ts
    - drizzle/migrations/ (at least one new .sql file)
  key_links:
    - sightings.userId → users.id (cascade)
    - sightings.speciesId → species.id (cascade)
    - collections.userId → users.id (cascade)
    - collections.speciesId → species.id (cascade)
    - collections uniqueIndex on (userId, speciesId) → enables upsert in 04-02
---

<objective>
Add `sightings` and `collections` tables to schema.ts and run the Drizzle migration against Neon.

Purpose: These tables are the data contract that all of Phase 4 (and Phase 5) build on. The `sightings` table is the immutable event log. The `collections` table is the per-user card unlock state. Both must exist before the API or beastiary page can be written.

Output: Updated schema.ts with both tables exported; migration SQL committed; `npx tsc --noEmit` passes; tables present in Neon.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-collection-mechanics/04-CONTEXT.md
@.planning/phases/04-collection-mechanics/04-RESEARCH.md
</context>

<interfaces>
<!-- Existing schema.ts exports the executor must not break or duplicate -->

From src/lib/db/schema.ts (current imports already cover all needed types):
```typescript
import {
  boolean, index, integer, pgEnum, pgTable,
  text, timestamp, uniqueIndex, uuid
} from "drizzle-orm/pg-core";
```

Existing exported tables: users, accounts, profiles, rarityTierEnum, sensitivityLevelEnum, species, occurrences

Key FKs to reference:
- users.id   — uuid PK
- species.id  — uuid PK
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add sightings and collections tables to schema.ts</name>
  <files>src/lib/db/schema.ts</files>
  <action>
    Append two new table definitions to the end of src/lib/db/schema.ts (after the occurrences table). Do NOT alter any existing table. All required imports (uuid, text, timestamp, integer, index, uniqueIndex) are already present at the top of the file.

    Add the sightings table first:
    - Table name: "sightings"
    - Columns: id (uuid PK defaultRandom), userId (uuid not-null FK → users.id cascade delete), speciesId (uuid not-null FK → species.id cascade delete), gridSquare (text not-null), sightedAt (timestamp defaultNow not-null)
    - No unique constraint — append-only event log, multiple rows per (userId, speciesId) are expected and correct.
    - Export as: export const sightings = pgTable("sightings", { ... });

    Add the collections table second:
    - Table name: "collections"
    - Columns: id (uuid PK defaultRandom), userId (uuid not-null FK → users.id cascade delete), speciesId (uuid not-null FK → species.id cascade delete), sightingCount (integer not-null default(1)), firstSightedAt (timestamp defaultNow not-null), lastSightedAt (timestamp defaultNow not-null)
    - Index config (second argument to pgTable): uniqueIndex("collections_user_species_idx").on(table.userId, table.speciesId) and index("collections_user_id_idx").on(table.userId)
    - Export as: export const collections = pgTable("collections", { ... }, (table) => ({ ... }));

    Use the exact Drizzle patterns already present in the occurrences table definition for the index config shape — copy the pattern, adjust names and columns.

    Per D-02 and D-03: gridSquare in sightings stores the client-supplied grid square string only. No GPS coordinates are present anywhere in the schema.
  </action>
  <verify>
    <automated>cd /Users/ethanvenables/Documents/Claude/Projects/Scout && npx tsc --noEmit</automated>
  </verify>
  <done>schema.ts exports sightings and collections; `npx tsc --noEmit` exits 0 with no errors.</done>
</task>

<task type="auto">
  <name>Task 2: Generate and run Drizzle migration</name>
  <files>drizzle/migrations/ (new SQL file generated here)</files>
  <action>
    Run the two-step Drizzle migration from the project root (/Users/ethanvenables/Documents/Claude/Projects/Scout):

    Step 1 — generate migration SQL:
      npx drizzle-kit generate

    Step 2 — apply to Neon (use DATABASE_URL_UNPOOLED, NEVER drizzle-kit push):
      DATABASE_URL=$DATABASE_URL_UNPOOLED npx drizzle-kit migrate

    If DATABASE_URL_UNPOOLED is not set in the shell environment, load it from .env.local first:
      export $(grep DATABASE_URL_UNPOOLED .env.local | xargs)
    Then re-run the migrate command.

    Do NOT use `drizzle-kit push` — it bypasses the migration file and is forbidden for this project.
  </action>
  <verify>
    <automated>cd /Users/ethanvenables/Documents/Claude/Projects/Scout && ls drizzle/migrations/*.sql | tail -5</automated>
  </verify>
  <done>At least one new .sql file exists in drizzle/migrations/ containing CREATE TABLE for sightings and collections; migration applied to Neon without error.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| schema → Neon | Migration SQL is generated from Drizzle source and applied via authenticated DATABASE_URL_UNPOOLED |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | drizzle-kit generate | accept | Migration is generated from the schema source; reviewed before apply |
| T-04-02 | Information Disclosure | DATABASE_URL_UNPOOLED in env | mitigate | Load from .env.local only; never commit to git |
| T-04-SC | Tampering | npm/npx installs | accept | drizzle-kit already installed in Phase 1; no new packages in this plan |
</threat_model>

<verification>
After both tasks complete:
- `npx tsc --noEmit` exits 0
- `ls drizzle/migrations/*.sql` shows a new migration file
- Migration file content includes `CREATE TABLE "sightings"` and `CREATE TABLE "collections"`
- `CREATE UNIQUE INDEX "collections_user_species_idx"` appears in the migration SQL
</verification>

<success_criteria>
- schema.ts exports `sightings` and `collections` with correct columns and constraints (per D-01)
- Drizzle migration SQL committed (per D-01)
- Tables exist in Neon (migration applied)
- TypeScript compiles cleanly — no errors introduced
- Raw GPS coordinates are absent from the schema (per D-03)
</success_criteria>

<output>
Create `.planning/phases/04-collection-mechanics/04-01-SUMMARY.md` when done.
</output>
