---
phase: 09-light-mode-card-art
plan: 03
type: execute
wave: 2
depends_on:
  - "09-01"
  - "09-02"
files_modified: []
autonomous: true
requirements:
  - POLISH-01
  - POLISH-02

must_haves:
  truths:
    - "tsc --noEmit exits 0 with no type errors"
    - "All imageUrl references in components match the updated SpeciesCardData and SpeciesResult types"
    - "BeastiaryClient passes imageUrl through to BeastiaryCard species prop"
  artifacts:
    - path: "src/types/discovery.ts"
      provides: "SpeciesResult with imageUrl (compiled clean)"
      contains: "imageUrl"
    - path: "src/components/beastiary/BeastiaryCard.tsx"
      provides: "SpeciesCardData with imageUrl (compiled clean)"
      contains: "imageUrl"
  key_links:
    - from: "BeastiaryPage speciesRows"
      to: "BeastiaryClient speciesRows prop"
      via: "imageUrl: s.imageUrl ?? null in the map"
      pattern: "imageUrl.*s\\.imageUrl"
    - from: "BeastiaryClient species spread"
      to: "BeastiaryCard species prop"
      via: "imageUrl passed in species object literal"
      pattern: "imageUrl.*s\\.imageUrl"
---

<objective>
Run `tsc --noEmit` to verify the full TypeScript build is clean after the changes in 09-01 and 09-02. Fix any type errors found — most likely causes: BeastiaryClient passing `species` object to BeastiaryCard without including `imageUrl`, or SpeciesRow receiving `sp: SpeciesResult` but the mini thumbnail referencing `sp.imageUrl` before the type was updated.

Purpose: Catch and fix type errors before the phase is marked complete.
Output: `tsc --noEmit` exits 0.
</objective>

<execution_context>
@/Users/ethanvenables/.claire/get-shit-done/workflows/execute-plan.md
@/Users/ethanvenables/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/ethanvenables/Documents/Claude/Projects/Scout/.planning/PROJECT.md
@/Users/ethanvenables/Documents/Claude/Projects/Scout/.planning/phases/09-light-mode-card-art/09-01-SUMMARY.md
@/Users/ethanvenables/Documents/Claude/Projects/Scout/.planning/phases/09-light-mode-card-art/09-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run tsc --noEmit and fix all type errors</name>
  <read_first>
    - Any file named in a tsc error output — read the specific file before fixing it
    - src/components/beastiary/BeastiaryClient.tsx — likely needs imageUrl added to the species object passed to BeastiaryCard (read if tsc reports errors here)
    - src/app/(app)/beastiary/page.tsx — likely needs imageUrl in the SpeciesRow type shape (read if tsc reports errors here)
  </read_first>
  <files>Any files emitting TypeScript errors (determined at runtime by tsc output)</files>
  <action>
Run the TypeScript compiler in no-emit mode:

```bash
cd "/Users/ethanvenables/Documents/Claude/Projects/Scout" && npx tsc --noEmit
```

For each error reported, identify the root cause and fix it. The most likely errors after 09-01 and 09-02:

**BeastiaryClient.tsx passing species to BeastiaryCard** — The inline object passed as `species` prop to `<BeastiaryCard>` currently spreads only the listed fields. After 09-02 added `imageUrl` to `SpeciesCardData`, the object literal in BeastiaryClient must include `imageUrl: s.imageUrl ?? null`. Read BeastiaryClient.tsx and add the missing field to the species object inside the `list.map()` in the card grid section.

**SpeciesRow type in BeastiaryClient** — The local `SpeciesRow` type (defined at top of BeastiaryClient.tsx as `SpeciesCardData & { sightingCount?; personalityTrait; isShiny; firstSightedAt }`) will automatically inherit `imageUrl` from the updated `SpeciesCardData` — no separate change needed. But if there is a mismatch, add `imageUrl?: string | null` to the local type explicitly.

**beastiary/page.tsx speciesRows type** — The inline object in `allSpecies.map(...)` needs to include `imageUrl: s.imageUrl ?? null`. This should already be done in 09-02 Task 1; if tsc still reports it missing, add it.

**SpeciesResult in DiscoverClient/SpeciesList** — These components receive `SpeciesResult[]` from props and pass down to SpeciesCard. The `imageUrl` field in SpeciesResult is optional (`imageUrl?: string | null`) so there should be no new type errors here — components that do not yet destructure imageUrl are unaffected.

After each fix, re-run `npx tsc --noEmit` until it exits 0.

Do not change any runtime behavior — only fix type annotations.
  </action>
  <verify>
    <automated>cd "/Users/ethanvenables/Documents/Claude/Projects/Scout" && npx tsc --noEmit && echo "TYPE CHECK PASSED"</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0 with no output (or only informational messages, no errors)
    - No "Property 'imageUrl' does not exist" errors in tsc output
    - No "Type ... is not assignable to type 'SpeciesCardData'" errors in tsc output
    - No regressions introduced — only type annotation fixes, no logic changes
  </acceptance_criteria>
  <done>`tsc --noEmit` exits 0. Phase 09 changes are type-safe.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tsc output | Compiler output only — no external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed — only tsc already in devDependencies |
</threat_model>

<verification>
Final phase verification:

```bash
cd "/Users/ethanvenables/Documents/Claude/Projects/Scout" && npx tsc --noEmit && echo "PHASE 09 TYPE CHECK CLEAN"
```

Expected: exits 0 with "PHASE 09 TYPE CHECK CLEAN".
</verification>

<success_criteria>
- `npx tsc --noEmit` exits 0 with no errors
- All type errors introduced by 09-01 and 09-02 changes are resolved
- No new type errors introduced during fixes
</success_criteria>

<output>
Create `.planning/phases/09-light-mode-card-art/09-03-SUMMARY.md` when done.
</output>
