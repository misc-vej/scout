---
phase: 09-light-mode-card-art
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/discovery.ts
  - src/app/api/discover/route.ts
  - src/app/(app)/beastiary/page.tsx
  - src/components/beastiary/BeastiaryCard.tsx
  - src/components/discover/SpeciesCard.tsx
  - data/species-seed.ts
  - data/seed.ts
autonomous: true
requirements:
  - POLISH-02

must_haves:
  truths:
    - "BeastiaryCard art zone shows the painterly PNG for Red Deer and Otter when imageUrl is present"
    - "BeastiaryCard DetailPanel art zone shows the PNG when imageUrl is present"
    - "SpeciesRow mini card thumbnail (44x62px) shows the PNG for Red Deer and Otter"
    - "Fox and other species without imageUrl show the SVG silhouette fallback"
    - "seed.ts upsert includes imageUrl so DB rows are updated on next seed run"
    - "API routes include imageUrl in species response objects"
  artifacts:
    - path: "src/types/discovery.ts"
      provides: "SpeciesResult type with imageUrl field"
      contains: "imageUrl"
    - path: "src/app/api/discover/route.ts"
      provides: "discover API response with imageUrl selected from species table"
      contains: "imageUrl: species.imageUrl"
    - path: "src/app/(app)/beastiary/page.tsx"
      provides: "Beastiary page query with imageUrl selected and passed to client"
      contains: "imageUrl"
    - path: "src/components/beastiary/BeastiaryCard.tsx"
      provides: "Art zone background-image when imageUrl present, SVG fallback when null"
      contains: "imageUrl"
    - path: "src/components/discover/SpeciesCard.tsx"
      provides: "Mini thumbnail background-image when imageUrl present"
      contains: "imageUrl"
    - path: "data/species-seed.ts"
      provides: "Red Deer imageUrl: /cards/red-deer.png, Otter imageUrl: /cards/otter.png"
      contains: "/cards/red-deer.png"
  key_links:
    - from: "data/species-seed.ts"
      to: "species DB row"
      via: "npm run db:seed upsert on scientificName conflict"
      pattern: "imageUrl.*red-deer"
    - from: "src/app/api/discover/route.ts"
      to: "SpeciesResult"
      via: "imageUrl: species.imageUrl in .select()"
      pattern: "imageUrl.*species\\.imageUrl"
    - from: "src/app/(app)/beastiary/page.tsx"
      to: "BeastiaryCard"
      via: "imageUrl in speciesRows array passed to BeastiaryClient"
      pattern: "imageUrl.*s\\.imageUrl"
    - from: "BeastiaryCard.tsx art zone"
      to: "public/cards/red-deer.png"
      via: "backgroundImage: `url(${species.imageUrl})`"
      pattern: "backgroundImage.*imageUrl"
---

<objective>
Wire up `imageUrl` from the species table into both card display surfaces: the BeastiaryCard grid/detail view and the SpeciesRow mini card thumbnail in the Nearby list. Update the type, both API routes, seed data, and components so that Red Deer and Otter show their painterly PNGs while all other species show the SVG silhouette fallback.

Purpose: Painterly card illustrations are already in `public/cards/` — this plan makes them visible per D-04/D-05/D-06.
Output: End-to-end imageUrl flow from seed data through API to card rendering.
</objective>

<execution_context>
@/Users/ethanvenables/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ethanvenables/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/ethanvenables/Documents/Claude/Projects/Scout/.planning/PROJECT.md
@/Users/ethanvenables/Documents/Claude/Projects/Scout/.planning/ROADMAP.md
@/Users/ethanvenables/Documents/Claude/Projects/Scout/.planning/phases/09-light-mode-card-art/09-CONTEXT.md
</context>

<interfaces>
<!-- Current SpeciesResult type (src/types/discovery.ts) — add imageUrl field:
export type SpeciesResult = {
  id: string;
  commonName: string;
  scientificName: string;
  rarityTier: string;
  sensitivityLevel: string;
  canBeShiny: boolean;
  taxonomyGroup: string | null;
  speciesType: string | null;
  recordCount: number;
  likelihood: number;
  isSeasonLocked: boolean;
  seasonUnlocksAt: string | null;
  imageUrl?: string | null;   // ADD THIS
};
-->

<!-- Current BeastiaryCard type (SpeciesCardData) — needs imageUrl added:
export type SpeciesCardData = {
  id: string;
  commonName: string;
  scientificName: string;
  rarityTier: string;
  speciesType: string | null;
  funFact: string | null;
  spottingTips: string | null;
  sensitivityLevel: string;
  no: string;
  habitat: string | null;
  imageUrl?: string | null;   // ADD THIS
};
-->

<!-- discover/route.ts SELECT object — add imageUrl field:
.select({
  id: species.id,
  ...existing fields...,
  imageUrl: species.imageUrl,   // ADD THIS
})
-->

<!-- beastiary/page.tsx SELECT — add imageUrl:
const allSpecies = await db
  .select({
    ...existing fields...,
    imageUrl: species.imageUrl,   // ADD THIS
  })
  .from(species)

// speciesRows map — include imageUrl:
imageUrl: s.imageUrl ?? null,
-->

<!-- seed.ts upsert set — currently does NOT include imageUrl; add it:
.onConflictDoUpdate({
  target: species.scientificName,
  set: {
    ...existing set fields...,
    imageUrl: entry.imageUrl ?? null,   // ADD THIS
  },
});
-->

<!-- species-seed.ts Red Deer entry (line 1576):
  commonName: "Red Deer",
  scientificName: "Cervus elaphus",
  imageUrl: null,   // CHANGE TO: imageUrl: '/cards/red-deer.png',
-->

<!-- species-seed.ts Otter entry (line 1682):
  commonName: "Otter",
  scientificName: "Lutra lutra",
  imageUrl: null,   // CHANGE TO: imageUrl: '/cards/otter.png',
-->

<!-- BeastiaryCard art zone pattern (D-04):
When imageUrl is non-null:
  - Set backgroundImage: `url(${species.imageUrl})` on art zone div
  - Set backgroundSize: 'cover', backgroundPosition: 'center'
  - Render AnimalIcon at opacity 0 (or omit it) — image replaces silhouette

When imageUrl is null:
  - Art zone renders as before (AnimalIcon SVG silhouette)
-->

<!-- SpeciesRow mini thumbnail pattern (D-04):
When sp.imageUrl is non-null:
  - Set backgroundImage: `url(${sp.imageUrl})` on the 44x62px div
  - Set backgroundSize: 'cover', backgroundPosition: 'center'
  - Do not render AnimalIcon child when image present

When sp.imageUrl is null:
  - Render as before (AnimalIcon child)
-->

<!-- DetailPanel art zone pattern:
When species.imageUrl is non-null:
  - height 180px div: set backgroundImage: `url(${species.imageUrl})`
  - backgroundSize: 'cover', backgroundPosition: 'center'
  - AnimalIcon rendered at opacity 0 or removed when image present
  - Tier badge and card number divs remain (position: absolute, zIndex: 3)
  - grain div remains

When species.imageUrl is null:
  - Render as before
-->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add imageUrl to types, API routes, and seed data</name>
  <read_first>
    - src/types/discovery.ts — current SpeciesResult shape (read before editing)
    - src/app/api/discover/route.ts — current SELECT and SpeciesResult usage (read before editing)
    - src/app/(app)/beastiary/page.tsx — current species SELECT and speciesRows map (read before editing)
    - data/seed.ts — current upsert set block (read before editing)
    - data/species-seed.ts — Red Deer entry (line ~1576) and Otter entry (line ~1682) (read both before editing)
    - src/lib/db/schema.ts — confirm imageUrl column exists at line 68 (imageUrl: text("image_url"))
    - .planning/phases/09-light-mode-card-art/09-CONTEXT.md — D-05, D-06
  </read_first>
  <files>src/types/discovery.ts, src/app/api/discover/route.ts, src/app/(app)/beastiary/page.tsx, data/seed.ts, data/species-seed.ts</files>
  <action>
**src/types/discovery.ts** — add `imageUrl?: string | null` as the last field of `SpeciesResult`. No other changes.

**src/app/api/discover/route.ts** — in the `.select({ ... })` call, add `imageUrl: species.imageUrl` alongside the existing fields. In the `filtered` map, the spread `...rest` should automatically carry imageUrl through; confirm the `SpeciesResult[]` type annotation still satisfies TypeScript. No other changes.

**src/app/(app)/beastiary/page.tsx** — in the `db.select({ ... })` call for `allSpecies`, add `imageUrl: species.imageUrl`. In the `speciesRows` array map, add `imageUrl: s.imageUrl ?? null` to each row object. In the `BeastiaryClient` speciesRows type (currently inline), this field will pass through to BeastiaryCard via the `species` prop — SpeciesCardData will be updated in Task 2.

**data/species-seed.ts** — find the Red Deer entry (commonName: "Red Deer", scientificName: "Cervus elaphus") and change its `imageUrl: null` to `imageUrl: '/cards/red-deer.png'`. Find the Otter entry (commonName: "Otter", scientificName: "Lutra lutra") and change its `imageUrl: null` to `imageUrl: '/cards/otter.png'`. All other entries remain `imageUrl: null`.

**data/seed.ts** — in the `.onConflictDoUpdate({ set: { ... } })` block, add `imageUrl: entry.imageUrl ?? null` so that running the seed updates existing rows' imageUrl values. Without this, the existing DB rows will not be updated even if re-seeded.

After all file edits are complete, run the seed to update the DB:
```bash
cd "/Users/ethanvenables/Documents/Claude/Projects/Scout" && npm run db:seed
```

If `DATABASE_URL_UNPOOLED` is needed, the package.json script uses `--env-file .env.local` so it should pick up the right env automatically.
  </action>
  <verify>
    <automated>cd "/Users/ethanvenables/Documents/Claude/Projects/Scout" && grep -n "imageUrl" src/types/discovery.ts src/app/api/discover/route.ts "src/app/(app)/beastiary/page.tsx" data/seed.ts data/species-seed.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/types/discovery.ts` contains `imageUrl?: string | null` in SpeciesResult
    - `src/app/api/discover/route.ts` SELECT block contains `imageUrl: species.imageUrl`
    - `src/app/(app)/beastiary/page.tsx` SELECT block contains `imageUrl: species.imageUrl`
    - `src/app/(app)/beastiary/page.tsx` speciesRows map contains `imageUrl: s.imageUrl ?? null`
    - `data/seed.ts` onConflictDoUpdate set contains `imageUrl: entry.imageUrl ?? null`
    - `data/species-seed.ts` Red Deer entry has `imageUrl: '/cards/red-deer.png'`
    - `data/species-seed.ts` Otter entry has `imageUrl: '/cards/otter.png'`
    - `npm run db:seed` exits 0 with "Seed complete" message
  </acceptance_criteria>
  <done>imageUrl flows from seed → DB → API response → component prop for both discover and beastiary routes.</done>
</task>

<task type="auto">
  <name>Task 2: Wire imageUrl display in BeastiaryCard and SpeciesCard components</name>
  <read_first>
    - src/components/beastiary/BeastiaryCard.tsx — SpeciesCardData type, CollectedCard art zone, LockedCard art zone, DetailPanel art zone (read before editing)
    - src/components/discover/SpeciesCard.tsx — SpeciesRow mini thumbnail div (44x62px, line ~44-64), ConfirmBanner mini thumbnail (read before editing)
    - .planning/phases/09-light-mode-card-art/09-CONTEXT.md — D-04 image display spec
  </read_first>
  <files>src/components/beastiary/BeastiaryCard.tsx, src/components/discover/SpeciesCard.tsx</files>
  <action>
**BeastiaryCard.tsx — SpeciesCardData type:**

Add `imageUrl?: string | null` to the `SpeciesCardData` type definition.

**BeastiaryCard.tsx — CollectedCard art zone:**

The art zone div currently contains `<AnimalIcon>` and `<div className="grain" />`. When `species.imageUrl` is non-null, set the art zone div's inline style to include `backgroundImage: \`url(${species.imageUrl})\``, `backgroundSize: 'cover'`, `backgroundPosition: 'center'`. When the image is present, do not render `<AnimalIcon>` (use a conditional). Keep `<div className="grain" />` regardless — it provides texture over the illustration.

Implementation pattern:
```
const hasImage = Boolean(species.imageUrl);
// Art zone div:
style={{
  ...existing styles,
  ...(hasImage ? {
    backgroundImage: `url(${species.imageUrl})`,
    backgroundSize: 'cover',
    backgroundPosition: 'center',
  } : {}),
}}
// Inside: {!hasImage && <AnimalIcon ... />}
// Always: <div className="grain" />
```

**BeastiaryCard.tsx — DetailPanel art zone:**

The 180px art zone currently renders `<AnimalIcon>` inside an absolutely positioned inner div. Apply same pattern: when `species.imageUrl` is non-null, set `backgroundImage`, `backgroundSize: 'cover'`, `backgroundPosition: 'center'` on the outer art zone div (the one with `height: 180`). Conditionally hide or omit the inner AnimalIcon div when image is present. Keep the tier badge, card number, and grain divs regardless (they are `position: absolute` and will overlay the image correctly).

**SpeciesCard.tsx — SpeciesRow mini thumbnail:**

The 44×62px div currently renders a child `<div style={{ width: '80%', height: '80%', opacity: 0.8 }}><AnimalIcon .../></div>`. When `sp.imageUrl` is non-null, add `backgroundImage: \`url(${sp.imageUrl})\``, `backgroundSize: 'cover'`, `backgroundPosition: 'center'` to the outer 44×62px div's style and do not render the inner AnimalIcon wrapper div. When null, render as before.

**SpeciesCard.tsx — ConfirmBanner mini thumbnail:**

The ConfirmBanner receives `species: SpeciesResult`. Apply the same pattern to its 40×56px thumbnail div: when `species.imageUrl` is non-null, use backgroundImage cover and omit the AnimalIcon child.

Do NOT change any rarity border colors, animation, or artBg values — those remain from getRarityConfig per D-02.
  </action>
  <verify>
    <automated>cd "/Users/ethanvenables/Documents/Claude/Projects/Scout" && grep -n "imageUrl\|backgroundImage\|background-image" src/components/beastiary/BeastiaryCard.tsx src/components/discover/SpeciesCard.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `BeastiaryCard.tsx` SpeciesCardData type contains `imageUrl?: string | null`
    - `BeastiaryCard.tsx` CollectedCard art zone div style includes conditional `backgroundImage` using `species.imageUrl`
    - `BeastiaryCard.tsx` DetailPanel art zone div style includes conditional `backgroundImage` using `species.imageUrl`
    - `BeastiaryCard.tsx` AnimalIcon in CollectedCard is conditionally rendered only when `!hasImage`
    - `SpeciesCard.tsx` SpeciesRow mini thumbnail div style includes conditional `backgroundImage` using `sp.imageUrl`
    - `SpeciesCard.tsx` ConfirmBanner thumbnail div includes conditional `backgroundImage` using `species.imageUrl`
    - `backgroundSize: 'cover'` and `backgroundPosition: 'center'` are set alongside every `backgroundImage`
    - `grain` div is always rendered in art zones (no conditional on it)
  </acceptance_criteria>
  <done>BeastiaryCard (grid card + detail panel) and SpeciesRow thumbnail display imageUrl as cover background when present, SVG silhouette when null. Red Deer and Otter show paintings; Fox and all others show SVG.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| imageUrl → CSS background-image | imageUrl values come from the DB (seeded by developer) — no user input populates this field |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-02-01 | Information Disclosure | backgroundImage: url(imageUrl) | accept | imageUrl values are developer-seeded paths to public/ assets; no user-controlled input reaches this property |
| T-09-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
After both tasks complete:

1. Confirm seed ran successfully:
```bash
cd "/Users/ethanvenables/Documents/Claude/Projects/Scout" && npm run db:seed
```
Expected: "Seed complete. N species upserted."

2. Confirm imageUrl is in all data layer files:
```bash
cd "/Users/ethanvenables/Documents/Claude/Projects/Scout" && grep -c "imageUrl" \
  src/types/discovery.ts \
  "src/app/api/discover/route.ts" \
  "src/app/(app)/beastiary/page.tsx" \
  data/seed.ts \
  data/species-seed.ts
```
Expected: all non-zero.

3. Confirm component wiring:
```bash
cd "/Users/ethanvenables/Documents/Claude/Projects/Scout" && grep -n "backgroundImage\|imageUrl" \
  src/components/beastiary/BeastiaryCard.tsx \
  src/components/discover/SpeciesCard.tsx
```
Expected: multiple matches showing the conditional backgroundImage pattern.

4. Confirm card images exist in public/:
```bash
ls "/Users/ethanvenables/Documents/Claude/Projects/Scout/public/cards/"
```
Expected: `red-deer.png` and `otter.png` present.
</verification>

<success_criteria>
- SpeciesResult type includes `imageUrl?: string | null`
- `/api/discover` response objects include `imageUrl` field
- Beastiary page query selects and passes `imageUrl` to BeastiaryClient
- Red Deer: `imageUrl` = `/cards/red-deer.png` in seed data and in DB after seed run
- Otter: `imageUrl` = `/cards/otter.png` in seed data and in DB after seed run
- BeastiaryCard CollectedCard art zone: PNG shown when imageUrl present, AnimalIcon when null
- BeastiaryCard DetailPanel art zone: same PNG/fallback logic
- SpeciesRow 44×62px thumbnail: same PNG/fallback logic
- ConfirmBanner 40×56px thumbnail: same PNG/fallback logic
- Fox and all non-illustrated species show SVG silhouette (imageUrl remains null)
</success_criteria>

<output>
Create `.planning/phases/09-light-mode-card-art/09-02-SUMMARY.md` when done.
</output>
