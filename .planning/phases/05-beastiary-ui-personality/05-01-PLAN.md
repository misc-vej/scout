---
plan: 05-01
title: funFact column on species + seed update
phase: 5
wave: 1
depends_on: []
req_ids: [BEAST-01]
branching_strategy: none
autonomous: true
files_modified:
  - src/lib/db/schema.ts
  - data/species-seed.ts
  - drizzle/migrations (generated)

must_haves:
  truths:
    - "D-01: species table has a nullable fun_fact text column in Neon"
    - "D-02: every seed entry in species-seed.ts has a distinct funFact value — no nulls, no placeholders"
    - "D-03: fun facts are playful and irreverent in tone — not generic biology summaries"
  artifacts:
    - src/lib/db/schema.ts
    - data/species-seed.ts
    - drizzle/migrations (new SQL file for ALTER TABLE species ADD COLUMN fun_fact)
  key_links:
    - "species-seed.ts InferInsertModel → schema.ts species table (funFact field must exist before seed re-run)"
    - "drizzle-kit migrate → Neon DATABASE_URL_UNPOOLED (migration must apply before db:seed)"
---

<objective>
Add a `funFact` text column to the species table, generate and apply the migration to Neon, then add a distinct playful fun fact to all ~115 species entries in the seed file and re-run the seed.

Purpose: Unlocked beastiary cards need a fun fact to display per species (D-01, D-02, D-03). This is the data layer — the card UI consumes it in Plan 05-03.

Output: `fun_fact` column live in Neon, all 115 species rows populated with irreverent UK wildlife facts.
</objective>

<execution_context>
@/Users/ethanvenables/Documents/Claude/Projects/Scout/.planning/phases/05-beastiary-ui-personality/05-RESEARCH.md
</execution_context>

<context>
@/Users/ethanvenables/Documents/Claude/Projects/Scout/.planning/ROADMAP.md
@/Users/ethanvenables/Documents/Claude/Projects/Scout/src/lib/db/schema.ts
@/Users/ethanvenables/Documents/Claude/Projects/Scout/data/species-seed.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add funFact to species schema and migrate</name>
  <files>src/lib/db/schema.ts, drizzle/migrations (new file)</files>
  <action>
    In `src/lib/db/schema.ts`, add `funFact: text("fun_fact"),` to the `species` pgTable definition — insert it after the `description` field (line 63) and before `imageUrl`. The column is nullable with no default.

    Then generate and apply the migration:

    Step 1 — generate migration SQL:
    ```
    npx drizzle-kit generate
    ```

    Step 2 — apply migration to Neon using the unpooled connection (required by drizzle-kit migrate):
    ```
    DATABASE_URL=$DATABASE_URL_UNPOOLED npx drizzle-kit migrate
    ```

    Confirm the migration ran without errors. The generated SQL file will contain `ALTER TABLE "species" ADD COLUMN "fun_fact" text;`.
  </action>
  <verify>
    <automated>node -e "const { drizzle } = require('drizzle-orm/neon-http'); const { neon } = require('@neondatabase/serverless'); const sql = neon(process.env.DATABASE_URL); const db = drizzle(sql); db.execute('SELECT fun_fact FROM species LIMIT 1').then(() => { console.log('PASS: fun_fact column exists'); process.exit(0); }).catch(e => { console.error('FAIL:', e.message); process.exit(1); });"</automated>
  </verify>
  <done>Migration applied, `fun_fact` column exists in the species table in Neon, schema.ts contains `funFact: text("fun_fact")`.</done>
</task>

<task type="auto">
  <name>Task 2: Add funFact to all 115 seed entries and re-run seed</name>
  <files>data/species-seed.ts</files>
  <action>
    Read the full `data/species-seed.ts` file. For every species entry in `seedData`, add a `funFact` field with a distinct, playful, irreverent 1–2 sentence UK wildlife fact. Write a unique fact for every single species — no nulls, no empty strings, no repeated facts.

    Tone rules (per D-03 and RESEARCH.md §2):
    - One to two sentences max
    - Irreverent but affectionate — never mean
    - UK-specific references where possible
    - Avoid generic biology facts — go for the surprising, funny, or emotionally resonant angle
    - Good patterns: "Scientists are baffled." / "Classic British X." / "One of the UK's most Y."

    Use the sample facts in RESEARCH.md §2 as the baseline for tone. Write NEW facts for those species rather than copying verbatim — extend the set with the same voice.

    Key species to hit with strong facts (beyond the RESEARCH.md samples):
    - Blackbird: males have jet-black plumage; females are brown and routinely mistaken for other species by beginners
    - Blue Tit: acrobatic, eats peanuts upside down, notoriously aggressive at feeders despite being the size of a walnut
    - House Sparrow: once so abundant it was considered a pest; now declining and red-listed. The sparrow did not see that coming.
    - Wren: Britain's most common breeding bird. Tiny, furious, impossibly loud for its size.
    - Magpie: one for sorrow, two for joy — a counting superstition attached to a bird that is simply trying to eat.
    - Kestrel: the motorway hawk. Can hold perfectly still in the air while scanning for voles below.
    - Swift: spends almost its entire life airborne — eats, sleeps, and mates on the wing. Lands only to breed.
    - Grey Heron: stands motionless for hours like a stone gargoyle then snaps up a fish in a millisecond.
    - Mute Swan: can break your arm, allegedly. This is mostly legend, but they want you to believe it.
    - Tawny Owl: makes the classic "twit-twoo" — except that's actually two owls calling to each other. You've been misled.
    - For fish, insects, plants if present — same irreverent tone, just UK-specific angles.
    - For reptiles (Grass Snake, Slow Worm, Common Lizard, Adder): lean into the "misunderstood underdog" angle.
    - For invertebrates: lean into the absurd (e.g. stag beetles spending 5+ years as larvae underground before a brief adult life).
    - For mammals: emotional resonance where possible (otters, hedgehogs, water voles).

    After updating every entry, run the seed script to populate Neon:
    ```
    npm run db:seed
    ```

    The seed uses `onConflictDoUpdate` on `scientificName` so this is safe and idempotent — it will update all 115 rows with their new funFact values.
  </action>
  <verify>
    <automated>npm run db:seed</automated>
  </verify>
  <done>All ~115 species entries in species-seed.ts have a non-null funFact string. `npm run db:seed` completes without errors. Spot-check: `SELECT common_name, fun_fact FROM species WHERE fun_fact IS NULL` returns 0 rows.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI → Neon (unpooled) | drizzle-kit migrate uses DATABASE_URL_UNPOOLED directly — ensure env var is set before running |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01-01 | Tampering | species-seed.ts funFact values | accept | Seed data is hardcoded strings, no user input; low-value target |
| T-05-01-02 | Denial of Service | drizzle-kit migrate on shared Neon free tier | accept | Single nullable column add; minimal lock duration |
</threat_model>

<verification>
1. `fun_fact` column exists in Neon: `SELECT fun_fact FROM species LIMIT 1` succeeds
2. No null funFacts: `SELECT COUNT(*) FROM species WHERE fun_fact IS NULL` returns 0
3. No duplicate funFacts: `SELECT fun_fact, COUNT(*) FROM species GROUP BY fun_fact HAVING COUNT(*) > 1` returns 0 rows
4. `npm run db:seed` exits 0
</verification>

<success_criteria>
- `fun_fact` column present in Neon species table (migration applied)
- All ~115 species rows have a distinct, non-null fun_fact value
- `npm run db:seed` runs without error (idempotent re-run)
- schema.ts compiles cleanly: `npx tsc --noEmit` passes
</success_criteria>

<output>
Create `.planning/phases/05-beastiary-ui-personality/05-01-SUMMARY.md` when done.
</output>
